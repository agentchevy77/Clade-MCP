# Independent QA Return — S03 RUN01 Final-Identity Digest Triage

Date: 2026-09-05
Packet reviewed: `NME001_QA_S03_RUN01_FINAL_IDENTITY_DIGEST_TRIAGE_2026-09-05.zip`, 4,007,701 B, SHA-256 `d45567ad…cd97` (verified); manifest and nested checkpoint v3 (`c046792f…`, 170 paths, identity file `db4737f0…`) verified. Context model: incoming QA baseline `a0c10bea…` + this delta.
Authority: this return grants no implementation, execution, evidence-promotion, historical-final, or release authority. Developer remains stopped pending Origin disposition.

## Classification

**`ROOT_CAUSE_CONFIRMED / NARROW_REPAIR_RECOMMENDED`**

## Findings against the eleven questions

1. **Canonical reproduction — YES.** The exact 630-date `session_identities` array in the frozen file, serialized by the established `exposure.session_identity_bytes` convention (compact JSON array, `sort_keys`, `ensure_ascii`, plus one LF), hashes to `423f3ad6…f429`. The file's own `session_identity_sha256`, `config/frozen_config.json` (`historical_final_session_identities_sha256`), and the loader's constant `FINAL_IDENTITIES_ORDERED_SHA256` (s02_execution.py:117) all carry that value. Independently reproduced in this QA lane on 2026-08-29 from the public XNYS calendar and again today from packet bytes.
2. **Divergent serializer — YES.** `load_frozen_final_identities` (s02_execution.py:676–678) hashes `"".join(f"{d.isoformat()}\n")` — newline-per-date — which over the same 630 dates yields exactly `ba899593…13b2`, then fails at line 680. Reproduced byte-exact.
3. **Test masking — YES.** `tests/test_s02_execution.py::test_final_identity_loader_validates_only_synthetic_identity_metadata` (line 574) builds a 4-date synthetic file, derives its expectation with the *same* newline-per-date expression (line 587), and monkeypatches `FINAL_IDENTITIES_ORDERED_SHA256` to it. Implementation and test agree with each other and never with the frozen contract. The real-file tests that assert `423f…` (`test_prior_exposure_dwo03`, `test_identity_dwo02:104`, `test_exposure_dwo04:134`) all go through the exposure/ledger path, never through this loader.
4. **Drift vs. conflict — LOCALIZED IMPLEMENTATION DRIFT, zero scientific-semantic effect.** The identity list, file bytes, frozen constant, and config are mutually consistent; only one validation guard's serializer disagrees. No authoritative conflict exists. Boundary of the drift is exactly one function: the *other* newline-per-date digests in `s02_execution` (`_date_sequence_sha256`, lines 307–310, 1072, 1077) are the correct convention for the S02 control projection — QA recomputed `empty_window_sessions_sha256`, `quarantine_sessions_sha256`, and `causal_successor_sessions_sha256` in the v4 control projection from their embedded session lists and all three match newline-per-date, none match compact-JSON. Those must not be touched. Nexus now has two legitimate conventions: governance session identities (compact JSON+LF) and S02 sequence digests (newline-per-date); the repair comments this at the seam.
5. **Corruption / drift / leakage / exposure.** Checkpoint corruption: NONE (outer and manifest hashes exact; identity file exact; source tree unchanged per stop record). Identity-list drift: NONE (630 dates, 2022-06-22..2024-12-31, canonical digest exact). Protected-data leakage inside the packet: NONE FOUND (no CSV/parquet/`data/raw` member; no market rows). Final-outcome exposure: the traceback places the failure inside `prepare_s02_development_source → load_frozen_final_identities`, before any Kibot prefix read or ledger parse, consistent with the stop record's `historical_final_payload_bytes_accessed: false`; physical non-access on the executor is `NOT_ASSESSED / INSUFFICIENT_SCOPE` for a sanitized packet.
6. **Smallest exact repair** (attached, unapplied): in `s02_execution.py`, import `session_identity_sha256` from `.exposure` (no import cycle — `exposure` imports only `calendar`/`partitions`) and replace the three-line hashlib expression with `digest = session_identity_sha256(identities)`; leave `_date_sequence_sha256` and every other serializer, the identity file, and the frozen constant untouched. 4 source lines changed net.
7. **Regression** (attached): (a) the synthetic test now derives its expectation via `session_identity_sha256`, so it can no longer agree with a wrong implementation; (b) one new real-artifact node `test_final_identity_loader_accepts_exact_frozen_governance_file` loads the actual frozen governance file with constants unpatched and asserts loader digest == constant == frozen config == `423f…`. **Inventory consequence:** +1 node in both lanes — hermetic 980→981, licensed-inclusive 1041→1042 (licensed-only stays 61); both ordered inventory digests (`8c9d737f…`, `d6adcdfc…`) must be re-frozen. Verified in an isolated copy: new test **fails** against the unpatched loader with the exact RUN01 exception; 2/2 targeted tests and 65/65 across `test_s02_execution`, `test_prior_exposure_dwo03`, `test_identity_dwo02`, `test_exposure_dwo04` **pass** on the patched copy.
8. **Established without licensed bytes or final outcomes — YES.** Everything above used only the governance identity file (session dates), source, tests, and config.
9. **New checkpoint and fresh lanes — YES.** Any source/test change alters `source_tree_sha256` (`8f53575b…`) and the named inventories; checkpoint v3 cannot carry the repair. A new prospective checkpoint (v4) with re-frozen inventories/digests, and every lane afresh under a new run identity, is required. RUN01 is consumed; its hermetic/licensed passes remain diagnostic only.
10. **Successor Developer inputs:** checkpoint v3 (`c046792f…`) as the exact baseline; this diff by hash; the frozen identity file and constants unchanged by contract; the S03 parent control and control projection unchanged; a fresh root with no lease-01/02/03 mutable material; competence in Python/pytest and the two-convention distinction in (4); boundary: apply exactly this diff, re-collect and freeze inventories, produce checkpoint v4 and stop — no licensed input until the hermetic lane passes, no final access, no other source edit.
11. **Secondary — 922/983 → 980/1041 RECONCILED.** Diffing the held DWO-05 named sets (Origin 922 JUnit; historical 983 JUnit) against `S02_TEST_INVENTORIES.json`: +58 nodes, 0 removed, in both lanes identically — `test_s02_execution.py` 30, `test_kibot_s02.py` 11, `test_corporate_actions_s02.py` 10, `test_s02_checkpoint.py` 4, `test_s02_supervisor.py` 3. Licensed-only remains 61 in both generations. No evidence gap.

## Attached diff

`NME001_QA_S03_RUN01_SERIALIZER_REPAIR_UNAPPLIED.diff` — 3,348 B, SHA-256 `45ec8717e5d5e1fbae059536b21e439d2ced9d7e9e1f9955800591dc0aafaaa0`. Unified diff against checkpoint v3 paths `src/nexus_nme001/s02_execution.py` and `tests/test_s02_execution.py`. Zero authority until Origin adopts it.

## Nonblocking notes

- Consider a one-line docstring on `_date_sequence_sha256` naming it the S02 control-projection convention, to prevent the reverse mistake later. Not included in the diff (scope discipline).
- The identical newline-per-date helper is duplicated in `development.py:183`, `stability.py:80`, `corporate_actions.py:174`; consolidation is hygiene, not a defect, and out of scope here.
