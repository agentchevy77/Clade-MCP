# NME-001 QA — S05-HQ01 F01 Review Findings Appendix (2026-09-06)

Candidate `4c6efd4e2564500efc542e26610d67a9059db4ce236e4dea08f66a34ce86a3bf`. Eight independent review lenses; each material finding was
checked by two adversarial refuters (one from the bytes, one from the charter text). Reviewer classifications are as reported; the
QA synthesis in the main report applies the refuters' corrections. Line numbers refer to members of the frozen return. Text is
trimmed for length; the complete reviewer output is retained by QA.

## Lens: supervisor

Hunk by hunk, the generalization preserves the donor's durable-probe strictness (production/supervisor.py:33-85 adds execution_identity/execution_class binding to the donor's run_identity+checkpoint set), the no-shell Popen with OSError-only "unable to launch" mapping (supervisor.py:217-228 vs EXACT_DONOR_SUPERVISOR.py:143-151), the STARTED/RUNNING/EXITED heartbeat loop with flush+fsync (supervisor.py:244-307), and PID/exit recording (now also a stage_status record, 231-243 and 311-315); it tightens path handling (absolute, non-symlink, real parent, exclusive stdout/stderr at 192-209 vs donor mkdir/resolve at 131-136). Fail-closed semantics are not weakened. A scratch copy confirmed the same bytes accept execution_class RUN with a non-null run_identity and reject class/identity mismatches, and that the absent-executable path raises before any STARTED line, leaving five zero-byte files exactly as case_results/F01/QN-06/artifacts/L/* and E/collection.* show. Records match code paths: QP-01 heartbeat has two STARTED (pids 26, 29) -> SUCCESS.json spawns 2; QN-05 one STARTED then EXITED 23 -> SUPERVISED_CHILD_EXIT_GATE; QN-06 empty L, stop boundary rewritten at harness.py:468 to POSTCOMMIT_CHILD_SPAWN_GATE with FileNotFoundError cause, first_child_spawn_attempts 1, spawns 0, re-entry 73. Supervisor sha e218664f... matches RETURN.md:44. Two real regressions versus the donor: child output now passes through pipes read in 64 KiB blocking chunks (supervisor.py:221-222, 260), so supervisor.log and stdout stay empty until exit or 64 KiB (the donor wrote the log file directly, donor 146-147), and reader.join() after the EXITED heartbeat (308-309) blocks indefinitely if a grandchild inherits the pipe. Heartbeat-STARTED spawn counting (harness.py:369-379) is sound given empty-L readiness and fsynced writes, but counts() runs inside the stop handler (474) so a corrupt heartbeat line would abort without a STOP record. No MATERIAL_BLOCKER.

**Not verifiable from bytes:**
- Whether T/durable_probe_readback.json ever came from an external durable target: driver.py:273-275 and 293-294 copy it from a developer-supplied --readback directory that is not in the return, so the 'external readback' is asserted, not proven by bytes (mitigation: both files are hash-bound in each …
- The RUNNING heartbeat branch (supervisor.py:283-294) is never exercised by the matrix (all supervised children exited in under 0.5 s); it was exercised only by my scratch test of a copy of supervisor.py.
- Behaviour of the in-process supervisor when harness.py itself is killed mid-child (pipe reader death, child EPIPE, loss of up to 64 KiB of unread output) cannot be established from the return; no case covers it.
- Genuineness of recorded PIDs/timestamps in case_results (PIDs 9-29 indicate a fresh PID namespace); the harness cannot be re-run here.
- The driver's spawn_count check (driver.py:343) compares the harness's self-reported synthetic_child_spawns to expectation and does not independently parse heartbeat.jsonl; I parsed the three heartbeat files myself and they agree (0, 1, 2 STARTED lines for QN-06, QN-05, QP-01).

### SG-01 — Pipe-based output capture delays child output until exit or 64 KiB; donor logged directly to file
Reported `NONBLOCKING_IMPROVEMENT` · production/supervisor.py:221-222, production/supervisor.py:258-265, EXACT_DONOR_SUPERVISOR.py:146-147, SUPERVISOR_GENERALIZATION.diff

Evidence: Donor: Popen(stdout=log_stream, stderr=subprocess.STDOUT) with the log opened buffering=0, so every child write landed in s04_supervisor.log immediately. Generalized: stdout=PIPE/stderr=PIPE with reader threads doing iter(lambda: pipe.read(65536), b'') on a BufferedReader; BufferedReader.read(n) blocks until n bytes or EOF. Scratch test on a copy: child 'echo early; sleep 2; echo late' -> stdout file and supervisor.log both 0 bytes at t=1 s, contents appear only at exit. QP-01/QN-05 supervisor.log contents are correct but were only written at child exit.

- Refuter (bytes): **STANDS** → `NONBLOCKING_IMPROVEMENT`. Confirmed from bytes. production/supervisor.py:221-222 launches with stdout=subprocess.PIPE/stderr=subprocess.PIPE at default bufsize (BufferedReader), and copy_output at :258-265 reads via iter(lambda: pipe.read(65536), b""); EXACT_DONOR_SUPERVISOR.py:146-147 instead passed stdout=log_stream (opened "ab", buffering=0) so child writes landed in the log immediately. A standalone Python reproduction in scratch (not the harness) with a child that prints 'early', sleeps 2 s, prints 'late' yielded zero chunks at t=1 s and a single combined chunk at t=2.01 s, proving BufferedReader.read(65536) …
- Refuter (charter): **STANDS** → `NONBLOCKING_IMPROVEMENT`. The charter clause the finding relies on exists verbatim (controls/CHARTER.md:131: supervisor "logging ... semantics may not be weakened"), and the defect is confirmed from the bytes and by scratch reproduction: production/supervisor.py:260 reads each child pipe with iter(lambda: pipe.read(65536), b"") on a BufferedReader, which blocks until 64 KiB or EOF, whereas the donor (EXACT_DONOR_SUPERVISOR.py:146-147) handed the unbuffered log file directly to the child so every write persisted immediately and survived supervisor death; in my repro an unbuffered child's line written at t=0 reached the …

### SG-02 — reader.join() after EXITED heartbeat can block forever if a grandchild inherits the pipe; elapsed and stage EXITED timing skew
Reported `NONBLOCKING_IMPROVEMENT` · production/supervisor.py:295-315, EXACT_DONOR_SUPERVISOR.py:170-181

Evidence: Loop exits on process.wait(), writes heartbeat EXITED (297-307), then joins both reader threads (308-309) before writing stage_status EXITED (311-315). Scratch test on a copy with child 'sleep 3 & exit 0': supervise returned 3.01 s after the child exited (no heartbeat during that gap), result elapsed_seconds=0.004, and stage EXITED observed_utc equals the pre-join exited_utc. Donor had no such wait because the child wrote a file.

- Refuter (bytes): **STANDS** → `NONBLOCKING_IMPROVEMENT`. The cited behavior is real and the line numbers are accurate. production/supervisor.py:218-226 launches the child with stdout=subprocess.PIPE and stderr=subprocess.PIPE, no start_new_session/preexec_fn/pass_fds control, so any grandchild that survives the child inherits the pipe write ends; lines 261-279 spawn two copy_output threads that loop on pipe.read() until EOF, and lines 308-309 call reader.join() with no timeout after the EXITED heartbeat (297-307) but before the stage_status EXITED record (311-315) and the return. exited_utc (295) and elapsed_seconds (296) are captured before the …
- Refuter (charter): **STANDS** → `NONBLOCKING_IMPROVEMENT`. Confirmed from bytes and an isolated scratch reproduction: production/supervisor.py:308-309 joins both pipe-reader threads with no timeout after the EXITED heartbeat (297-307) and before the stage EXITED record (311-315); a child `sh -c 'echo hi; sleep 3 & exit 0'` held supervise() for 3.01 s with elapsed_seconds=0.004, versus 0.00 s when the grandchild closed its inherited fds, whereas the donor (EXACT_DONOR_SUPERVISOR.py:147-181) wrote the child fd straight to the log and returned immediately after the EXITED heartbeat, so the window is new in the generalization. No charter or startup-order …

### SG-03 — Heartbeat-STARTED spawn counting is sound but brittle: parsed inside the stop handler, unfiltered, and duplicated by unused in-memory counters
Reported `NONBLOCKING_IMPROVEMENT` · production/harness.py:369-379, production/harness.py:464-478, production/harness.py:361, production/supervisor.py:239-253

Evidence: counts() re-reads heartbeat.jsonl and json.loads() every line (373-374) with no execution_identity/stage filter; it is invoked at 474 inside the except handler, so a truncated/corrupt line would raise out of the handler and no STOP record would be emitted. self.synthetic_spawns (361) is incremented but never read. In supervise, stage_status STARTED (239-243) precedes heartbeat STARTED (244-253); a write failure between Popen and the heartbeat record propagates without terminating the running child, leaving stage_status showing STARTED while counts() reports 0.

- Refuter (bytes): **STANDS** → `NONBLOCKING_IMPROVEMENT`. All cited behaviors are present in the bytes: production/harness.py:372-374 json.loads every heartbeat line unfiltered (twice per line) and is called at harness.py:474 inside the except handler and at harness.py:385 in the replay path outside any try, so a corrupt residual heartbeat.jsonl on re-entry raises JSONDecodeError past the handler and no STOP.json/replay_rejection.json is emitted (still fail-closed, exit nonzero, no commit); self.synthetic_spawns (harness.py:112,361) is never read; and production/supervisor.py:239-253 writes stage_status STARTED before heartbeat STARTED with no …
- Refuter (charter): **REFUTED** → `OBSERVATION`. The finding's factual observations are accurate from the bytes (production/harness.py:361 `self.synthetic_spawns` is written at 112/361 and never read; counts() at 369-379 json.loads() every heartbeat line with no identity/stage filter and is called at 474 inside the except handler; supervisor.py:239-253 writes stage_status STARTED before heartbeat STARTED), but none of them constitutes a real gap under the charter, so NONBLOCKING_IMPROVEMENT is not proportionate. (1) The no-filter concern is moot: heartbeat.jsonl is descriptor-bound under root L whose write-set entry is `before: …

**Observations (no action):**
- SG-04 — Supervisor topology changed from a separate 300 s process over the whole sequence to an in-process per-stage call with a 30 s hard-coded interval
- SG-05 — QN-06 'unable to launch' path verified; routing is a string match and pre-Popen file opens leave zero-byte stage outputs for a stage never reached
- SG-06 — RUN-class acceptance confirmed; durable probe binds identity/class/checkpoint only and its qualification readback is a local copy
- SG-07 — Strictness increases and stdout/stderr separation are genuine improvements over the donor
- SG-08 — Supervisor bytes and case records are internally consistent with the narrative

## Lens: writeset

Readiness is sound where production owns it, but the whole-write-set reconciliation is driver-owned. Mapping (production/harness.py): entries = descriptor write_set (106-107); item 1 construct PRECOMMIT dirs (222-229); item 2 lstat after construction (50-54 via real_dir, mode at 233); item 3 type/containment/separation (52, 147-171 topology + resolve alias, 162-163 duplicates); item 4 probe_dir create/write/fsync/rename/parent-fsync/read/delete + lexists residue check (71-91), file targets probe parent only (241-246), P/V parents probed before construction (241-246, 417); item 5 targets bound only via write_set_sha256 (247-248, recomputed and matches readiness.json) not as absolute paths in the receipt; item 6 V-parent probe (417, unreceipted); item 7 rejections (170-171, 176-178, 200-203, 236-243, 305-312). Receipt is realized state (lstat/lexists), derived from the single write set.

Checked against QP-01: all 44 readiness entries in write-set order, 42 realized per declared after, stop/replay_rejection ABSENT; every artifact path is a declared target. But: self.receipts (post-readiness per-stage observations) is only persisted on STOP (472); SUCCESS.json (460-462) carries none, and no production step at COMPLETE reconciles the 44 entries. That final reconciliation is qualification/driver.py:361-372 (informational) plus exact_artifacts from the driver's copied artifact_map (100-135, 338-341). uv_project_lock is observed only if it exists (273-277). Undeclared files in D/E/L/O/TMP/MPL are never scanned by production (only Q at 214, managed trees at 286). A real run without the driver has no success-path write-set receipt and no undeclared-output detection.

**Not verifiable from bytes:**
- Transient files created and removed by uv/pytest under TMP or P during execution (no live filesystem; only final state and receipts are in the return).
- Actual removal of probe files beyond what receipts assert (residue_absent is code-derived at harness.py:84-85; cannot re-run the harness).
- MPL writability beyond the readiness probe (no matplotlib in the fixture, MPL never written; case_results contain no MPL directory).
- Whether the pre-state of E/L/O/D/TMP/MPL was actually ABSENT at invocation for QP-01/QP-02: only inferred structurally from driver.py:277-279 (fresh B with exist_ok=False), never observed by production or driver.
- Whether uv 0.11.33 always leaves the TMP project lock behind (the only evidence is that it existed at the post-sync observation in all cases that reached UV_SYNC).

### RWS-01 — Success-path per-stage observations are discarded; whole-write-set final reconciliation exists only in the qualification driver
Reported `NONBLOCKING_IMPROVEMENT` · production/harness.py:136, production/harness.py:460-462, production/harness.py:472, qualification/driver.py:361-372, qualification/driver.py:100-135, qualification/driver.py:338-341

Evidence: harness.py:136 appends every observe_output/readiness result to self.receipts; the SUCCESS payload at 460-462 contains only status/identity/processes/counts (verified: SUCCESS.json keys are authority, execution_identity, first_child_spawn_attempts, input_byte_drift, processes, protected_contact_count, qualification_commits, run_attempt_commits, run_identity, scientific_episode_count, status, synthetic_child_pids, synthetic_child_spawns). self.receipts is written only into STOP.json as partial_write_observations (472). The 44-entry final_write_set_observations in result.json is produced by driver.py:361-372 and is informational (not in checks); the exact_artifacts check (338-341) compares …

- Refuter (bytes): **STANDS** → `NONBLOCKING_IMPROVEMENT`. The finding stands with two corrections. Confirmed from bytes: production/harness.py appends every observe_output result to self.receipts (the append is at line 135, not 136; 136 is `return observed`); the success payload at harness.py:460-462 carries only status/identity/authority/processes/input_byte_drift/counts, and case_results/F01/QP-01/artifacts/O/SUCCESS.json has exactly those 13 keys with no write-set entries; self.receipts is serialized only into the STOP payload as partial_write_observations (harness.py:472). No COMPLETE-time loop over self.entries exists in production; the only …
- Refuter (charter): **REFUTED** → `OBSERVATION`. The finding's code citations are accurate (harness.py:136 appends observe_output records to self.receipts; the SUCCESS payload at 460-462 omits them; they appear only in STOP.json at 472; driver.py:361-372 produces the 44-entry final_write_set_observations), but the defect it infers does not exist as stated and the charter imposes no requirement it violates. (1) "Per-stage observations are never persisted on success" is false: readiness() writes self.receipts into D/readiness.json (harness.py ~226-228) and the persisted QP-01 readiness.json holds one PASS row for every one of the 44 write-set …

### RWS-02 — uv_project_lock (and any future same-stage auxiliary output) is observed only if it exists; a missing target after its owning stage is not rejected by production
Reported `NONBLOCKING_IMPROVEMENT` · production/harness.py:273-277, production/harness.py:418-429, qualification/driver.py:123-129, case_specs/F01/QP-01/descriptor.json (write_set id uv_project_lock, stage UV_SYNC, after EXISTS_IF_STAGE_REACHED)

Evidence: The F01 repair loop at 273-277 observes same-stage file entries only when exists(entry['path']) is true. Nothing else in execute() references uv_project_lock. In HQ the driver's prelaunch required list (driver.py:123-125) is what enforces its presence; production alone would have passed with it absent.

- Refuter (bytes): **STANDS** → `NONBLOCKING_IMPROVEMENT`. Confirmed from bytes. production/harness.py:273-277 (the entire F00->F01 delta) observes same-stage 'file' entries only when exists(entry['path']) is true; grep over production/ shows uv_project_lock is never referenced by name and no other loop in Harness iterates EXISTS_IF_STAGE_REACHED entries after their stage (readiness() at 219-248 only checks pre-state; execute() at 418-429 continues from UV_SYNC to precommit/commit with no existence check for the lock; success at 457-460 does no final write-set reconciliation). descriptor.schema.json and the controls define no weaker semantics for …
- Refuter (charter): **STANDS** → `NONBLOCKING_IMPROVEMENT`. The finding stands. Charter section 6 item 7 (controls/CHARTER.md:166) exists verbatim as relied on: the production harness must "reject any target that disagrees with its stage-specific declared state, including a target missing after its owning construction stage." case_specs/F01/QP-01/descriptor.json declares uv_project_lock as kind=file, stage=UV_SYNC, after=EXISTS_IF_STAGE_REACHED, and UV_SYNC is reached in every commit-reaching case. In production/harness.py the only code touching that entry is the F01 repair loop at 273-277, which calls observe_output only when `exists(entry['path'])` …

### RWS-03 — Production never scans the non-managed roots E, L, O, D, TMP, MPL for undeclared outputs; only Q and the managed trees P/V reject undeclared descendants
Reported `NONBLOCKING_IMPROVEMENT` · production/harness.py:214, production/harness.py:231, production/harness.py:286-306, production/harness.py:430, qualification/driver.py:323-328, qualification/driver.py:340

Evidence: grep of production/harness.py for rglob/iterdir/scandir/os.walk/listdir yields exactly three sites: 214 (Q rglob, PROVENANCE_GATE on undeclared source member), 231 (emptiness of PRECOMMIT dirs at readiness only), 286 (os.walk inside managed_observe for P and V). Undeclared-descendant rejection at 305-306 applies to managed trees only. The driver's rglob over D/E/L/O/TMP/MPL (323-328) feeding exact_artifacts (340) is the only undeclared-output detector, and it is qualification code.

- Refuter (bytes): **STANDS** → `NONBLOCKING_IMPROVEMENT`. Every cited site is real and at the stated line: production/harness.py:214 is the only rglob and it walks Q (PROVENANCE_GATE 'undeclared source member'); :231 is `any(p.iterdir())` guarded by before=='ABSENT_OR_EXISTS_WRITABLE_EMPTY' inside readiness(), so it runs once before any child; :286-306 is os.walk inside managed_observe(), which is invoked only for 'public_cache' (P) and 'virtual_environment' (V), and the 'unexpected' rejection at :305-306 requires a member_contract with required_members; :430 is the Q-only stray check `exists(self.roots['Q']/'.venv')`. grep of production/*.py for …
- Refuter (charter): **REFUTED** → `OBSERVATION`. The code facts in RWS-03 are accurate: production/harness.py scans a tree at exactly three sites (214 Q rglob, 231 precommit emptiness via iterdir, 286 os.walk inside managed_observe), undeclared-descendant rejection at 305-306 is reached only for entries of kind managed_tree (P and V, the only two such entries in every descriptor), the six E/L/O/D/TMP/MPL directory entries carry after=EXISTS_WRITABLE with no member_contract, post-stage observation (271-277, 363-364) touches only declared file ids, emit() at 121 guards only harness-authored keys, SUCCESS.json (QP-01 artifacts/O/SUCCESS.json) …

### RWS-04 — Readiness receipt records no absolute target paths; charter item 5 is met only indirectly through write_set_sha256 and descriptor hash
Reported `NONBLOCKING_IMPROVEMENT` · production/harness.py:232-233, production/harness.py:238, production/harness.py:245-246, production/harness.py:247-248, case_results/F01/QP-01/artifacts/D/readiness.json, case_results/F01/QP-01/artifacts/D/precommit.json

Evidence: All 44 readiness.json entries lack a path key (verified count 0); directory entries expose the path only inside probe.directory, reserved-file entries expose only parent_probe.directory, protected entries carry id/observed/status only. write_set_sha256 (05002399...) recomputes exactly from the descriptor's write_set; precommit.json and the commit bind readiness identity and descriptor_sha256.

- Refuter (bytes): **STANDS** → `NONBLOCKING_IMPROVEMENT`. Confirmed from the bytes: harness.py:233-234/240/245-246 build readiness receipts with no path key (0 of 44 entries in every one of the 7 readiness.json files carry a path); the absolute heartbeat, supervisor-log, stage-status, stop-record, package, and scientific-output targets appear in no harness-emitted success-path receipt and are recoverable only from the descriptor, bound via write_set_sha256 (recomputes exactly for all 7 cases) and the commit's descriptor_sha256 (matches the shipped descriptor.json for all 4 commits). The harness does directly record absolute stdout/stderr …
- Refuter (charter): **STANDS** → `NONBLOCKING_IMPROVEMENT`. Finding confirmed and slightly understated. CHARTER.md:158-164 makes item 5 ("record absolute stdout, stderr, JUnit, heartbeat, log, stage-status, scientific-output, package, and stop-record targets") an explicit production-harness obligation. readiness.json in QP-01, QP-02 and QN-05 contains 0 path keys across 44 entries; reserved-file entries carry only parent_probe.directory (harness.py:242-246). The only code that records absolute file targets, observe_output (harness.py:125-135), runs after readiness is emitted and its receipts are dropped on the success path (emit('success') at …

### RWS-05 — Pre-state of the six PRECOMMIT roots (absent versus pre-existing empty) is neither observed by production nor by the driver; invocation.json copies it from expectation
Reported `NONBLOCKING_IMPROVEMENT` · production/harness.py:225-233, qualification/driver.py:302-304, qualification/driver.py:308, case_results/F01/QP-01/invocation.json, case_results/F01/QP-02/invocation.json

Evidence: readiness() accepts before=ABSENT_OR_EXISTS_WRITABLE_EMPTY and records no created/pre-existed flag (225-233). driver.py:302-304 asserts only S, C, P, V absent before invocation; driver.py:308 writes initial_evidence_state from expected['initial_evidence_state'], and both QP invocation.json files show ABSENT from that source, not from an lstat.

- Refuter (bytes): **STANDS** → `NONBLOCKING_IMPROVEMENT`. All cited behavior confirmed at the cited lines: harness.py:226-234 evaluates exists(p) but discards it and the readiness receipt for E in case_results/F01/QP-01/artifacts/D/readiness.json carries only mode/observed_type/probe/status; driver.py:302-304 checks only S,C,P,V; driver.py:308 copies expected['initial_evidence_state'], which is itself a hardcoded literal at driver.py:243 ('REGULAR_FILE' if case=='QN-01' else 'ABSENT'), into invocation.json beside an observed_utc timestamp; result.json checks (11 keys) contain no E pre-state check. No code elsewhere observes or records E's pre-state. …
- Refuter (charter): **REFUTED** → `OBSERVATION`. The facts are correct (harness.py:222-234 records no created/pre-existed flag; driver.py:302-304 lstat-asserts only S,C,P,V; driver.py:308 copies initial_evidence_state from expected, hardcoded at driver.py:243), but the charter rule the finding invokes does not exist as stated. Section 6 requires the harness to "create every precommit writable parent it owns" and "observe each with lstat after construction", and declares a receipt invalid only if produced "without post-construction observation"; the QP readiness receipts (artifacts/D/readiness.json, entry E: observed_type directory, lstat …

**Observations (no action):**
- RWS-06 — Stop-record staging D is constructed only because it is first in the write-set order; R is a precondition production does not create
- RWS-07 — Several realized checks are performed but not receipted: V-parent probe before sync, V-absence check, and the pre-command parent probes
- RWS-08 — Write-set entry paths for root ids are not cross-checked against the roots map; harness mixes roots[] and write-set paths for P, V, Q, T
- RWS-09 — Positive confirmations for this lens

## Lens: commit

Attempt-commit boundary is coherent for HQ01; no MATERIAL_BLOCKER. Vector verdicts: (a) CLOSED for HQ01: commit copies descriptor production/fixture hashes (production/harness.py:336-337) but binds descriptor_sha256 (336); I recomputed both hashes from the QN-06 inputs map and they match, and return production/* bytes match the inputs; harness itself never recomputes (improvement). (b) OPEN in production, closed for HQ01 only by qualification/driver.py:263,280: check_inputs walks Q not T (205-217); sys.path[0]=T/production, no -P; -B does not stop reading a planted __pycache__ pyc; an undeclared T/production/json.py or hashlib.py would subvert every check. (c) CLOSED fail-closed: crash after launch.lock (328) before commit re-enters into readiness 'nonempty precommit root' (230) STOP with commits 0, truthful. (d) CLOSED: exists() (383) treats a partial commit as consumed, correct direction. (e) PARTIALLY OPEN: exclusive creates serialize, but the loser's STOP suppresses the winner's later STOP (474) and a second re-entry raises FileExistsError outside the try (384), exit 1 not 73; no spawn either way. (f) OPEN-ish: counts (369-379) trust heartbeat STARTED lines; supervisor.py:204,212-216 append to a pre-existing file; no cross-check vs in-memory processes (QA cross-checked all 8 cases: consistent). (g) CLOSED: commit at 440 after precommit emit 435, supervised at 441, no child marker read. (h) CLOSED: line 330 keys on execution_class; 173-176; all descriptors QUALIFICATION; run_commit absent in all 8; but emit writes before consulting kind/after (118-123, 341-343). (i) GAP for the RUN order: no order-identity field; lease_identity unchecked. (j) YES: replay path 383-387 spawns nothing; QN-06 shows commit 1 (1881 B), STOP first_child_spawn_attempts 1 with ENOENT, heartbeat/stage_status/log/collection.* all 0 bytes, synthetic_child_spawns 0, re-entry 73.

**Not verifiable from bytes:**
- Contents of T at execution time (undeclared modules, sitecustomize, __pycache__ pyc files): not in the return; only the 20 declared inputs are hash-bound.
- Bytes of the interpreter /opt/codex/.../bin/python and uv binary: bound by path and version string only (harness.py:189-195, 393-396); inputs must lie under B (209) so they cannot be declared.
- That the launched harness.py was T/production/harness.py: no sys.orig_argv or __file__ identity is recorded in any commit/STOP/SUCCESS; the only evidence is qualification/driver.py:311 source, which is excluded from descriptor inputs (driver.py:217) and bound only by …
- Actual fsync durability to media: cannot be verified from bytes.
- Concurrent-process and second-re-entry behaviour: not exercised by the fixed matrix; verdicts are from code reading only.
- Whether the QN-06 Popen ENOENT involved a real fork before exec failure: no PID recorded (by design, supervisor.py:227-228); the 'one failed OS spawn attempt' claim rests on the OSError text in STOP.json underlying_error.

### AC-01 — Provenance check walks Q for undeclared members but not T; an undeclared shadow module or planted pyc under T/production would defeat every hash check
Reported `NONBLOCKING_IMPROVEMENT` · production/harness.py:7-18, production/harness.py:205-217, qualification/driver.py:263, qualification/driver.py:280, qualification/driver.py:291, case_specs/F01/QN-06/descriptor.json (environment: 17 keys, no PYTHONPATH, no PYTHONSAFEPATH)

Evidence: check_inputs (205-217) hashes only declared inputs and rglobs only roots['Q'] for undeclared files. The launcher is run as 'python -B T/production/harness.py' (driver.py:311), so sys.path[0] is T/production and 'from cache import', 'from supervisor import' (17-18) plus stdlib imports json/hashlib/subprocess (7,5,12) resolve there first. -B / PYTHONDONTWRITEBYTECODE=1 only suppress writing pyc; a pre-existing T/production/__pycache__/cache.cpython-312.pyc with matching source mtime/size is still loaded. T also receives the whole extracted cache package (driver.py:291), which is why T cannot be fully enumerated.

- Refuter (bytes): **STANDS** → `NONBLOCKING_IMPROVEMENT`. Confirmed from the bytes, not refuted. production/harness.py:17-18 does bare `from cache import ...` / `from supervisor import ...` and the driver launches it as `[PYTHON, '-B', T/production/harness.py, ...]` (qualification/driver.py:311) with no `-P`; the descriptor environment is exactly 17 keys (LANG, LC_ALL, MKL_NUM_THREADS, MPLCONFIGDIR, NUMEXPR_NUM_THREADS, OMP_NUM_THREADS, OPENBLAS_NUM_THREADS, PATH, PYTEST_DISABLE_PLUGIN_AUTOLOAD, PYTHONDONTWRITEBYTECODE, PYTHONHASHSEED, TMPDIR, TZ, UV_NO_CONFIG, UV_OFFLINE, UV_PROJECT_ENVIRONMENT, XDG_CACHE_HOME) with no PYTHONSAFEPATH, so …
- Refuter (charter): **REFUTED** → `OBSERVATION`. The code facts in AC-01 are accurate (production/harness.py:205-217 hashes only declared inputs and rglobs only roots['Q']; driver.py:311 runs 'python -B T/production/harness.py' so sys.path[0] is T/production; -B does not stop CPython from loading a pre-existing __pycache__ .pyc), but the defect it asserts does not exist under the controlling text. (1) No charter or startup-order clause requires the production harness to self-enumerate T for undeclared members: CHARTER section 5 asks for a "hash-bound reusable production harness bundle" and section 9 asks the commit to "bind ... harness" — …

### AC-02 — Spawn counts derive from heartbeat STARTED lines in an append-mode file that is never checked absent at its owning stage and is never cross-checked against in-memory process records
Reported `NONBLOCKING_IMPROVEMENT` · production/harness.py:369-379, production/harness.py:346-367, production/supervisor.py:202-216, production/supervisor.py:244-253, case_results/F01/QN-06/artifacts/L/heartbeat.jsonl (0 bytes), case_results/F01/QN-05/artifacts/L/heartbeat.jsonl

Evidence: counts() (369-379) parses every line of L/heartbeat.jsonl with status STARTED. supervise() admits a pre-existing regular file at heartbeat/stage_status/log (supervisor.py:204) and opens them 'a'/'ab' (212-216); readiness (233-244) checks RESERVED_ABSENT targets only at PRECOMMIT time, and supervised() (349-350) only probes parents. Children run as the same UID with write access to L and D. The harness keeps independent in-memory records (self.synthetic_spawns 360, self.processes 361, spawn_attempt_count 352) but never compares them with the heartbeat-derived count. QA cross-check: in all 8 cases heartbeat STARTED pids equal terminal-record synthetic_child_pids and the in-memory …

- Refuter (bytes): **STANDS** → `NONBLOCKING_IMPROVEMENT`. Confirmed from bytes: production/harness.py:369-375 derives synthetic_child_spawns only from STARTED lines in L/heartbeat.jsonl; self.synthetic_spawns (harness.py:112, :361) is incremented but never read or compared, and self.processes is emitted but never reconciled with the heartbeat count; supervised() at harness.py:349-350 only probes parent directories, the sole RESERVED_ABSENT check is readiness() at :241-243 before the inheritance/bootstrap/sync children run (:402-419), and the F01 same-stage observation (:273-277) does not cover SUPERVISOR-stage files; production/supervisor.py:204 …
- Refuter (charter): **REFUTED** → `OBSERVATION`. The code reading is accurate (readiness() at harness.py:390 runs before the pre-commit children at 405-425; supervisor.py:204/212-216 admit and append to a pre-existing heartbeat file; counts() at harness.py:369-379 never reconciles against self.synthetic_spawns/self.processes), but no charter or startup-order requirement for a second absence check at the owning stage or for a heartbeat-vs-memory cross-check exists. CHARTER section 9 defines HERMETIC_COLLECTION_PROCESS_STARTED as a separately recorded durable record after OS child creation with PID, which is exactly the STARTED line written …

### AC-03 — emit()/commit() write the target before consulting its declared kind/postcondition, so a protected path (e.g. RUN_ATTEMPT_COMMIT.json) could be materialized before the gate fires
Reported `NONBLOCKING_IMPROVEMENT` · production/harness.py:118-123, production/harness.py:125-129, production/harness.py:326-344, qualification/driver.py:173-176, case_results/F01/*/artifacts/D (RUN_ATTEMPT_COMMIT.json absent in all eight)

Evidence: emit (118-123) checks only membership in targets, then write_exclusive, then observe_output; observe_output (125-129) is where after != EXISTS_IF_STAGE_REACHED (i.e. a protected entry) is rejected. commit() (341-343) follows the same order for commit_key. commit_key is chosen by execution_class at line 330, and QUALIFICATION descriptors are forced to run_identity null / no 'RUN' in execution_identity (173-176); every HQ01 descriptor is QUALIFICATION and run_commit is declared protected ABSENT (driver.py:173-176). All eight terminal records show run_attempt_commits 0 and the file is absent.

- Refuter (bytes): **STANDS** → `NONBLOCKING_IMPROVEMENT`. Confirmed from bytes: production/harness.py commit() calls write_exclusive(self.path(commit_key)) at line 341 and sets commit_count=1 at 342 before observe_output(commit_key) at 343, and observe_output (126-129) is the only place the entry's after=='EXISTS_IF_STAGE_REACHED' is enforced; emit() (120-124) has the same write-then-check order. No earlier gate closes this: validate_descriptor (139-198) never checks the commit key's kind/after, readiness/check_protected only assert protected paths are absent, execute line 383 only rejects pre-existing commits, and descriptor.schema.json:33 leaves …
- Refuter (charter): **STANDS** → `OBSERVATION`. The code ordering is confirmed: production/harness.py:118-123 (emit) and 326-343 (commit) call write_exclusive before observe_output, which at 125-129 is the first check of entry['after']. But the finding's characterization as a real gap is not supported. (1) Reachability: every emit() key is a literal that is non-protected in the driver's write set; the only route to a protected target is commit() with commit_key 'run_commit', which needs execution_class RUN, while the return's sole descriptor author hardcodes QUALIFICATION (qualification/driver.py:210,227) and harness.py:173-176 …

### AC-04 — Concurrent launch and second re-entry are fail-closed for spawning but can leave a non-truthful or missing durable stop record
Reported `NONBLOCKING_IMPROVEMENT` · production/harness.py:57-68, production/harness.py:326-329, production/harness.py:381-387, production/harness.py:459-478, production/harness.py:448-455

Evidence: There is no entry-level lock; launch.lock is created only inside commit() (328). Two processes on one descriptor serialize at the first exclusive create (readiness emit, 245) where the loser gets FileExistsError, caught at 459-478 and written as STOP.json (boundary = self.boundary, commits 0). The winner continues; if it later fails after commit, line 474 'if not exists(stop)' skips writing its own STOP, leaving a root with a commit and a STOP that says commits 0 at a readiness boundary; if it succeeds, SUCCESS.json and STOP.json coexist (contradicting charter section 7 'no stop record'). Line 448-455 does not check STOP absent before SUCCESS. A second re-entry after replay_rejection.json …

- Refuter (bytes): **STANDS** → `NONBLOCKING_IMPROVEMENT`. Confirmed from production/harness.py: no entry lock exists (launch.lock only at 328-329 inside commit()); write_exclusive (57-68) uses 'xb' so a concurrent loser fails at mkdir 228, nonempty-root 231-232, RESERVED_ABSENT 241-243, or readiness emit 247 and lands in the except at 464-478, whose 'if not exists(stop)' at 475 both lets the loser write a STOP with commits observed as 0 (counts(), 375) and later suppresses the winner's own STOP after a post-commit failure; commit() at 438 and the success emit at 460-462 have no STOP-absent precondition, so SUCCESS.json and STOP.json can coexist. The …
- Refuter (charter): **STANDS** → `NONBLOCKING_IMPROVEMENT`. The code behavior is confirmed from the bytes: production/harness.py creates launch.lock only inside commit() (line 329), emits replay_rejection at line 384 outside the try at 388 (so a second re-entry raises FileExistsError from write_exclusive's 'xb' open at line 60 and exits 1 with a traceback instead of 73), emits SUCCESS at line 460 with no STOP-absent precondition, and skips its own STOP at line 475 when one already exists. However, the charter does not require what the finding recommends: section 9 requires the commit under the single-writer launch lock, which the exclusive …

### AC-05 — Commit provenance is by declaration: production/fixture hashes are copied from the descriptor, the launcher does not bind its own executing identity, and interpreter/uv bytes cannot be declared
Reported `NONBLOCKING_IMPROVEMENT` · production/harness.py:330-343, production/harness.py:205-210, production/harness.py:189-195, production/harness.py:393-396, qualification/driver.py:217, qualification/driver.py:311

Evidence: commit() takes checkpoint_sha256 and production_sha256 from self.d (336-337) and never recomputes them from the inputs map; the descriptor's own bytes are bound (descriptor_sha256, 336) and QA recomputed sha256(canonical({relname: record})) over the five T/production inputs and the four fixture inputs of the QN-06 descriptor: both equal the declared values (bf3c9811..., 723d254c...), and the return's production/* bytes equal those records. check_inputs requires every input under B (209), so /opt/codex python and uv are bound only by path/version strings (189-195, 393-396). Nothing in the commit records sys.orig_argv or identity(__file__); the code that chooses to launch …

- Refuter (bytes): **STANDS** → `NONBLOCKING_IMPROVEMENT`. Confirmed from bytes: production/harness.py:335-336 sets checkpoint_sha256 and production_sha256 directly from self.d['fixture_sha256'] / self.d['production_sha256'], and validate_descriptor (139-178) plus every other production module contains no code that recomputes either value from the inputs map (grep for production_sha256/fixture_sha256 hits only the copy sites 335/360/396 in harness.py and the driver). check_inputs (205-213) verifies declared paths under B only; the interpreter is bound by sys.executable == runtime['python'] (191) and uv by its --version string (393-396), never by …
- Refuter (charter): **STANDS** → `NONBLOCKING_IMPROVEMENT`. The finding stands; every factual claim is confirmed from the bytes and the classification is proportionate. production/harness.py:335-336 copies checkpoint_sha256 and production_sha256 verbatim from self.d and nothing recomputes them; check_inputs (207-213) confines inputs to B so python/uv are bound only by path/version/platform strings (191-195, 393-396); no payload records identity(__file__) or sys.orig_argv; the launch of T/production/harness.py is driver.py:311 and driver.py is excluded from descriptor inputs (217-219). I independently recomputed sha256(canonical(...)) with the …

**Observations (no action):**
- AC-06 — Replay rejection record mixes root-derived and process-local counters; it does not validate or cite the existing commit
- AC-07 — No field binds the future Origin RUN order identity; lease_identity and several descriptor values are not validated
- AC-08 — Commit ordering and QN-06 evidence satisfy charter sections 7 and 9; spawn-attempt counter is conservative

## Lens: ledger

Ledger reconciliation from artifacts only: ZERO mismatches on all 11 items across all 8 cases (script s05work/ledger-recon/recon.py). Final and frozen ledgers agree on identities, descriptor and expected_artifacts hashes.

Case | terminal (artifact=ledger=charter) | commit | HB STARTED | outer/reentry | req/forb/extra
QP-01 | O/SUCCESS.json | 1 | 2 [26,29] | 0/- | 28/0/0
QP-02 | O/SUCCESS.json | 1 | 2 [26,29] | 0/- | 28/0/0
QN-01 | STOP WRITE_SET_READINESS_GATE | 0 | none | 1/- | 1/0/0
QN-02 | STOP INHERITED_CWD_GATE | 0 | none | 1/- | 5/0/0
QN-03 | STOP INHERITED_ENVIRONMENT_GATE | 0 | none | 1/- | 5/0/0
QN-04 | STOP EXACT_PUBLIC_CACHE_GATE | 0 | none | 1/- | 7/0/0
QN-05 | STOP SUPERVISED_CHILD_EXIT_GATE exit 23 | 1 | 1 [26] | 1/- | 22/0/0
QN-06 | STOP POSTCOMMIT_CHILD_SPAWN_GATE, replay new_spawn_attempts 0 | 1 | 0-byte file | 1/73 | 23/0/0

Also confirmed: no RUN_ATTEMPT_COMMIT/PROTECTED_READY/scientific_output/package.zip anywhere incl. history/F00; QP junit exactly one testcase test_synthetic, 0 failure/error/skipped, inventory exactly ['test_synthetic.py::test_synthetic']; commit descriptor_sha256 == sha256(case_specs descriptor) == both ledgers, production/checkpoint == ledger, commit command/env == descriptor, commit created_utc precedes first STARTED; readiness.write_set_sha256 == sha(descriptor.write_set); all artifact copies hash-match result.json; 8 unique identities, run_identity null everywhere; STOP/SUCCESS exclusive; ledger totals (4 commits, 5 spawns, 6 supervisor, 9 outer invocations) sum; F00 history matches prior_candidate. Residual gaps are recorded as nonblocking findings.

**Not verifiable from bytes:**
- Outer harness process argv/cwd/env at invocation: no record captures them; only driver.py:311-313 code and the harness gate (harness.py:189-198) imply env==descriptor.
- Realized P and V trees are not in the return; only receipts (cache_observation, venv_observation) are.
- Timestamps (commit created_utc, heartbeat observed_utc) are self-reported by the same process; ordering is consistent but not externally attested.
- native_probe_readback_source path in invocation.json is a claim; the readback tree is not in the return.
- PID namespace inference (outer_pid 7 and identical child PIDs in every case) cannot be confirmed from bytes.
- Harness could not be executed (runtime paths and 148MB cache absent).

### LR-01 — invocation.json does not bind outer argv, cwd, or environment
Reported `NONBLOCKING_IMPROVEMENT` · qualification/driver.py:305-313, case_results/F01/QP-01/invocation.json, production/harness.py:189-198

Evidence: invocation.json keys are only descriptor, fixture_sha256, initial_evidence_state, native_probe_readback_source, observed_utc, production_sha256, protected_roots_absent, record_identity, run_identity, state. driver.py:311-313 launches harness.py with cwd=T and env=d['environment'] but records none of it.

- Refuter (bytes): **STANDS** → `OBSERVATION`. The bytes confirm the factual claim: qualification/driver.py:305-310 writes invocation.json with only the ten keys listed, lines 311-313 Popen harness.py with cwd=roots['T'] and env=d['environment'] unrecorded, and production/harness.py only compares os.environ/sys.orig_argv/Path.cwd at lines 191-198 without writing them anywhere (the commit's 'environment' at harness.py:337 is the descriptor copy, result.json adds only outer_pid/outer_exit). However it is not a real gap in qualification soundness: CHARTER.md:218 defines ORCHESTRATION_INVOCATION as plumbing with no binding requirement, …
- Refuter (charter): **REFUTED** → `OBSERVATION`. The factual observation is correct (qualification/driver.py:305-313 writes invocation.json without argv/cwd/env), but the requirement LR-01 relies on does not exist as stated and the record is not gameable. Charter s9's "normalized environment" binding applies to RUN_ATTEMPT_COMMIT and s7 requires the qualification commit only to exercise exclusive-create/fsync/readback/replay-rejection mechanics; even so, production/harness.py:337 binds environment into QUALIFICATION_CASE_COMMIT.json (verified byte-equal to the descriptor environment for QP-01), plus descriptor_sha256 (covering runtime, …

**Observations (no action):**
- LR-02 — protected_roots_absent in invocation.json is a constant literal, and QN-01 has no harness-written S/C absence record
- LR-03 — QN-02/QN-03 STOP boundary name is taken from child-written inheritance.stdout
- LR-04 — QN-06 counters and zero-byte supervised outputs need cross-record reading
- LR-05 — PIDs repeat across cases (outer_pid 7; children 9/11/14/26/29 in QP-01, QP-02, QN-05)

## Lens: purity

PRODUCTION PURITY: harness.py, supervisor.py, cache.py, inherit.py contain no case names, fault hooks, os.environ/argv switches, or qualification-only paths (grep plus full read). The only descriptor-keyed branches are the charter-mandated execution_class QUALIFICATION/RUN (harness.py:114-121, 331-340; supervisor.py:170-174). Heuristics noted, all fail-closed and not case-keyed: substring 'RUN' check (harness.py:115), hard-coded python3.12 site-packages (harness.py:424), boundary from error-string match 'unable to launch' (harness.py:468).

EXTERNALITY: every perturbation is imposed by qualification/driver.py before the unchanged entrypoint runs: QN-01 pre-creates E as a regular file (driver.py:300-301); QN-02 descriptor cwd (:189); QN-03 grandchild argv (:185-186); QN-04 bootstrap wrapper (:201-203); QN-05 fail_child (:204-205); QN-06 absent executable (:206-207) with re-entry (:318-321). Launch is [PYTHON, -B, T/production/harness.py, T/descriptor.json], cwd=T, env=descriptor environment (driver.py:311-313). Artifacts confirm each named boundary.

FREEZE INTEGRITY: inputs maps are identical across all 8 descriptors for the 5 production, 4 qualification, 3 fixture and 5 cache-control entries (only durable probes differ, as expected). freezes/F01/PRODUCTION_HARNESS.zip holds exactly the 5 production members. F00 to F01: harness.py +5 lines (272a273-277) only; driver.py hash changed; uv_path_contract.py new; supervisor/cache/inherit/schema/fixture byte-identical; repair_refreeze_cycles=1 of 2. production_sha256 bf3c9811 and fixture_sha256 723d254c reproduce via sha256(canonical(records(dir))) (driver.py:32-50, 62-63); F00 41d8063f reproduces from _f00. F00 QP-01 is a FAIL under different harness bytes and is excluded from the 8 PASS rows. Exactly the charter's eight cases exist.

No MATERIAL_BLOCKER found in this lens.

**Not verifiable from bytes:**
- F00 driver.py bytes and the F00 QP-01 descriptor are not in the return (only hashes in history/F00/FROZEN_MANIFEST.json and history/F00/QP01_FAILED_RESULT.json); the driver-side portion of REPAIR_01 changes 1, 2 and 4 is inferred from F01 driver content (driver.py:158-164, :16/:144, :374) and …
- Actual runtime execution could not be reproduced (no runtime paths, no 148MB cache); all execution claims rest on the returned artifacts, which are hash-bound but were produced by the developer's environment.
- QN-01's executed-byte binding is attested only by the driver (driver.py:295-298, result.json executed_inputs_before/after), because production stops at readiness before check_inputs (harness.py:389-390); no production-emitted inputs_before.json exists for QN-01. The other seven cases carry …
- The uv interpreter-cache key derivation (interpreter-v4/e128016a93bda1d6/7f8106be2b9d4caa.msgpack) remains undocumented by the developer (UV_LOCK_DERIVATION.md, REPAIR_01.json cache_key_scope); I could not derive it from bytes either. The uv project-lock basename derivation was independently …
- Whether the developer's rolling WIP Library versions (CUSTODY_READBACK_V*.json) correspond to the returned bytes is outside this lens and not checkable offline.

### PP-01 — Commit binds descriptor-copied production_sha256/checkpoint_sha256 rather than digests recomputed from the realized trees
Reported `NONBLOCKING_IMPROVEMENT` · production/harness.py:335-336, production/harness.py:360, production/harness.py:396, qualification/driver.py:213-221, qualification/driver.py:235, case_results/F01/QP-01/artifacts/D/QUALIFICATION_CASE_COMMIT.json

Evidence: harness.py reads self.d['production_sha256'] and self.d['fixture_sha256'] only to copy them into the commit binding and the durable-probe check; it never recomputes sha256(canonical(records(T/production))) or the fixture digest from Q. Per-file identities are verified by check_inputs (harness.py:146-158), but the consistency between the tree digest and the per-file entries is established solely by driver.plan() populating both from the same FROZEN_MANIFEST. I recomputed both digests externally and they match (bf3c9811..., 723d254c...).

- Refuter (bytes): **STANDS** → `NONBLOCKING_IMPROVEMENT`. Confirmed from the bytes. production/harness.py:335-336 builds the commit binding with 'checkpoint_sha256': self.d['fixture_sha256'] and 'production_sha256': self.d['production_sha256'], and lines 360 and 396 pass self.d['fixture_sha256'] straight into supervise()/verify_durable_probe(); grep over production/*.py shows these are the only uses of either key — there is no records()/canonical() tree-digest recomputation anywhere in production/. The digests originate solely in qualification/driver.py:62-63 (freeze) and are copied into each descriptor at driver.py:235; descriptor.schema.json types …
- Refuter (charter): **REFUTED** → `OBSERVATION`. The factual observation is accurate (harness.py:335-336 copies descriptor-supplied production_sha256/fixture_sha256 and never recomputes a tree digest), but the requirement the finding relies on does not exist as stated and the behaviour is authorized. Charter section 9 requires the commit to "bind" the harness/checkpoint/descriptor, not to recompute them; section 6's "observed realized state" language governs the write-set receipts, which the commit satisfies via identity() of realized readiness/precommit files; section 5 places run-specific values in the descriptor, section 8 forbids …

### PP-02 — Descriptor command/inheritance surfaces are untyped in the schema and unconstrained by production; QN-04/05/06 rely on exactly this freedom
Reported `NONBLOCKING_IMPROVEMENT` · production/descriptor.schema.json (properties.commands, inheritance, runtime, environment, inputs, cache all {"type":"object"}), production/harness.py:191-201, production/harness.py:257-261, production/harness.py:346-360, qualification/driver.py:201-207

Evidence: The schema types commands/inheritance/runtime/environment/inputs/cache only as object. harness.command() and harness.supervised() execute spec['argv'] with spec['cwd'] verbatim; there is no check that bootstrap argv is the frozen bootstrap_uv_cache.py, that collection/test argv begin with V/bin/python -m pytest, or that inheritance cwd equals Q. The driver imposes QN-04, QN-05 and QN-06 by substituting these argv values.

- Refuter (bytes): **STANDS** → `NONBLOCKING_IMPROVEMENT`. Confirmed from bytes: production/descriptor.schema.json types commands/inheritance/runtime/environment/inputs/cache as bare {"type":"object"}; harness.validate_descriptor (production/harness.py:138-199) checks only field-set membership, roots topology, write_set entries, environment and authority, never commands[*].argv/cwd or inheritance; harness.command (:260) calls subprocess.Popen(spec['argv'], cwd=spec['cwd'], ...) and harness.supervised (:353-360) calls supervise(spec['argv'], ..., cwd=spec['cwd']) verbatim from descriptor['commands']; qualification/driver.py:201-207 imposes QN-04/05/06 …
- Refuter (charter): **REFUTED** → `OBSERVATION`. The factual observations are accurate (production/descriptor.schema.json types commands/inheritance/runtime/environment/inputs/cache only as {"type":"object"}; production/harness.py:260 and :353-360 pass spec['argv']/spec['cwd'] to Popen/supervise verbatim; qualification/driver.py:201-207 substitutes argv for QN-04/05/06). But the requirement the finding relies on -- that the schema or production should constrain argv shape, executable-under-V, or cwd==Q -- does not exist in the charter, and the charter affirmatively requires the opposite. CHARTER.md section 7 states "All perturbations must …

### PP-03 — Qualification-only helpers are copied into T/qualification and hash-bound as inputs in every case, including both positives
Reported `NONBLOCKING_IMPROVEMENT` · qualification/driver.py:213-219, qualification/driver.py:281-284, case_specs/F01/QP-01/descriptor.json (inputs: T/qualification/{bootstrap_missing_link,fail_child,uv_path_contract,venv_contract}.py), freezes/F01/PRODUCTION_HARNESS.zip

Evidence: driver.run copies every qualification/*.py except driver.py into T/qualification for all 8 cases, and driver.plan binds those four files in every descriptor's inputs map (verified identical hashes across all 8). The frozen production bundle zip contains exactly cache.py, descriptor.schema.json, harness.py, inherit.py, supervisor.py and nothing else.

- Refuter (bytes): **STANDS** → `OBSERVATION`. The factual core is confirmed from the bytes: qualification/driver.py:281-284 (run) copies every qualification/*.py except driver.py into T/qualification unconditionally for all 8 cases, and driver.py:213-219 (plan) binds those four files into every descriptor's inputs map (all 8 descriptors carry the same four hashes c91ebf68/eabb7354/3205cb81/8e5ef7fa); freezes/F01/PRODUCTION_HARNESS.zip contains only cache.py, descriptor.schema.json, harness.py, inherit.py, supervisor.py. Nothing elsewhere in the return closes the gap (no case-conditional copy, no narrative rationale in …
- Refuter (charter): **REFUTED** → `OBSERVATION`. The observed behavior is confirmed from the bytes (qualification/driver.py:213-219 binds T/qualification/{bootstrap_missing_link,fail_child,uv_path_contract,venv_contract}.py into every descriptor's inputs; driver.py:281-284 copies them into T for all 8 cases; freezes/F01/PRODUCTION_HARNESS.zip contains only the five production members), but no controlling requirement is violated or even implicated. The only relevant charter text is section 7: "Qualification-only controls must never enter the production bundle" -- the bundle is the frozen zip, which the reviewer concedes is clean. Section 6 …

### PP-04 — The F01 harness addition observes same-stage outputs only if present; presence of declared EXISTS_IF_STAGE_REACHED files and absence of undeclared files under E/L/O/D/TMP/MPL is enforced by the driver, not production
Reported `NONBLOCKING_IMPROVEMENT` · production/harness.py:273-277, qualification/driver.py:322-341, qualification/driver.py:125, _f00/harness.py vs _f01/harness.py (diff 272a273-277)

Evidence: The only production change F00->F01 is a loop that calls observe_output for file entries of the owning stage that exist(); nothing is raised if a declared file (e.g. uv_project_lock) is absent. Production checks undeclared members only in Q (check_inputs) and in the managed trees P and V (managed_observe); the exact required/forbidden artifact set for E/L/O/D/TMP/MPL is computed by the driver after the run (driver.py:338-341).

- Refuter (bytes): **STANDS** → `NONBLOCKING_IMPROVEMENT`. The cited bytes confirm the core of PP-04 but the finding is overbroad in one respect. Confirmed: (a) the only F00->F01 production change is harness.py:273-277, which observes same-stage file entries only when exists(entry['path']) is true and raises nothing when a declared file is absent; (b) production has no directory sweep of E/L/O/D/TMP/MPL — grep shows the only enumerations are Q (harness.py:214 check_inputs), the pre-run emptiness check (231), managed_observe's os.walk over P/V (286), and cache.py:180 scandir of the cache — so an undeclared child write into TMP/D/E after readiness is …
- Refuter (charter): **REFUTED** → `OBSERVATION`. The code facts in PP-04 are accurate (production/harness.py:273-277 observes same-stage file entries only when exists(); production sweeps for undeclared members only in Q via check_inputs at harness.py:214-216 and in the managed trees P/V via managed_observe at harness.py:286-306; the exact required/forbidden set for D/E/L/O/TMP/MPL is computed by qualification/driver.py:322-341), but the requirement the finding rests on does not exist as stated and the behavior is what the charter prescribes. (1) CHARTER.md:191 locates the "exact expected/forbidden artifact manifest" at the case level …

**Observations (no action):**
- PP-05 — QN-01 boundary attribution comes from a generic RuntimeError labelled with the running boundary marker, not an explicit GateError
- PP-06 — QN-01 executed-byte binding is driver-attested only
- PP-07 — F00 to F01 change set is consistent with REPAIR_01's four listed changes; only the harness change is byte-verifiable
- PP-08 — QP-02's 'different absolute prefix' shares the HQ ancestor with QP-01
- PP-09 — Non-case-keyed heuristics in production worth recording
- PP-10 — Externality and launch path verified per case; fault injection confined to each case's own realization

## Lens: cache

(1) The 148,782,742-byte archive is hash-checked only by the qualification driver (qualification/driver.py:271-272, against a hard-coded constant) and extracted unverified (driver.py:287-291). Production never reads descriptor cache.archive/archive_sha256 (only package_root, production/harness.py:410); PACKAGE_MEMBER_MANIFEST.json is hash-pinned but never consumed (production/cache.py:15,112-113). The effective gate is the realized-cache content check. (2) Yes: cache.py:176-216 scandir/lstat, readlink, O_NOFOLLOW SHA-256 of all 8,928 files, exact path sets, literal link targets, counts. QN-04's stop is the production CacheError 'symlink path-set mismatch: missing=[wheels-v6/pypi/contourpy/...]' (case_results/F01/QN-04/artifacts/D/STOP.json) while the wrapper exited 0 and the bootstrap receipt claimed PASS/31 links, which cache.py ignores. (3) Required 9,800 = 8,928 hashed payload files + 1 unhashed receipt + 839 payload dirs + 'uv' + 31 symlinks; cache_write_observation has exactly 9,800 members with 9 optional absent; cache_post_sync has 9,809 with 0 absent and zero changed rows. Optional: uv/.gitignore, uv/.lock, uv/CACHEDIR.TAG, uv/interpreter-v4, uv/interpreter-v4/e128016a93bda1d6, .../7f8106be2b9d4caa.msgpack, uv/sdists-v9, uv/sdists-v9/.git, uv/sdists-v9/.gitignore. (4) Promotion happened between executions by refreeze, exact names, no wildcards; consistent with charter section 6 and REPAIR_01. Prefix invariance is observed (5 realizations, 3 prefixes, 2 freezes), and I reproduced the shard directory e128016a93bda1d6 = SeaHash('x86_64'+FF, 'Linux'+FF, '6.18.35'+FF) using the return's own routine: it is kernel-release-dependent, not path-dependent; the harness does not gate kernel release. (5) A key change stops at harness.py:429 (WRITE_SET_GATE) before commit at :438: commits 0, spawns 0, unconsumed. (6) No: optional directories' contents remain subject to the exact set (harness.py:305). (7) Confirmed: five distinct basenames recomputed and observed. Limitation: NONBLOCKING_IMPROVEMENT.

**Not verifiable from bytes:**
- bootstrap_uv_cache.py bytes are inside the excluded 148 MB archive, not in the return; I could not confirm its copy/link semantics or that it never consults a prior receipt (only that production/cache.py ignores receipts).
- The msgpack file name 7f8106be2b9d4caa was not reproduced from any candidate encoding of the bound interpreter path (python, python3, python3.12; component, 0xff-terminated, length-prefixed, raw forms) using the return's SeaHash routine; its derivation remains unknown, as the developer disclosed.
- The shard-directory reproduction (e128016a93bda1d6 from arch/os_type/kernel release 6.18.35) is arithmetic evidence with the return's own uv_path_contract._seahash, not confirmation against uv 0.11.33 source (no network); the exact os_release string sys_info reports on a future host cannot be …
- Whether uv 0.11.33 writes additional interpreter-cache keys on another host (e.g. a second msgpack for a venv interpreter) cannot be determined from four realizations on one host.
- The F00 descriptor is not in the return; that uv/.lock, uv/.gitignore and uv/CACHEDIR.TAG were already optional in F00 is inferred from history/F00/QP01_STOP.json listing only the six interpreter-v4/sdists-v9 members as unexpected.
- The harness could not be executed (runtime paths and cache absent); all statements about real-run stop ordering are from static reading of production/harness.py.

### CG-01 — Interpreter-cache shard directory is derivable and depends on kernel release, which the harness does not gate
Reported `NONBLOCKING_IMPROVEMENT` · UV_LOCK_DERIVATION.md:52-56, REPAIR_01.json:2, qualification/uv_path_contract.py:27-32, production/harness.py:191-195, production/harness.py:429, RUNTIME_PROVENANCE.json:6

Evidence: Using the return's own SeaHash routine (uv_path_contract._seahash, which reproduces all five observed uv-*.lock basenames), SeaHash(b'x86_64\xff' + b'Linux\xff' + b'6.18.35\xff') little-endian hex = e128016a93bda1d6, exactly the declared optional directory uv/interpreter-v4/e128016a93bda1d6. RUNTIME_PROVENANCE.json platform is Linux-6.18.35-x86_64-with-glibc2.39. harness.py:191-195 gates python path/version, implementation, system, machine and libc, but not platform.release(). The developer declared this derivation UNKNOWN (UV_LOCK_DERIVATION.md:52-56, REPAIR_01.json cache_key_scope). The msgpack file name 7f8106be2b9d4caa was not reproduced from the interpreter path.

- Refuter (bytes): **STANDS** → `NONBLOCKING_IMPROVEMENT`. Confirmed from bytes: qualification/uv_path_contract.py _seahash over b'x86_64\xff'+b'Linux\xff'+b'6.18.35\xff' yields e128016a93bda1d6, the exact optional_members shard key in the QP-01 descriptor (and 6.18.36 yields 10deff7b727eae58, so the name moves with uname -r); production/harness.py:191-195 gates python path/version/implementation/system/machine/libc but nothing in production/ or qualification/ reads platform.release() or the kernel string (descriptor has zero occurrences of 6.18.35; RUNTIME_PROVENANCE.json:6 is narrative only); harness.py:300-306 raises WRITE_SET_GATE for any …
- Refuter (charter): **STANDS** → `NONBLOCKING_IMPROVEMENT`. Confirmed from the bytes: the return's own SeaHash routine (qualification/uv_path_contract.py:27-32) over b'x86_64\xff'+b'Linux\xff'+b'6.18.35\xff' yields e128016a93bda1d6, exactly the declared optional shard uv/interpreter-v4/e128016a93bda1d6 in every F01 descriptor, and neighbouring kernel strings yield different names; production/harness.py:191-195 gates system/machine/libc/python but never platform.release(); managed_observe (harness.py:304-306) raises WRITE_SET_GATE on any unexpected member (history/F00/QP01_STOP.json:1 is the precedent on these same paths) and cache_post_sync …

### CG-02 — Descriptor cache.archive_sha256 is not enforced by production; archive-to-package_root extraction is never verified
Reported `NONBLOCKING_IMPROVEMENT` · production/harness.py:410, production/cache.py:12-19, production/cache.py:96-113, qualification/driver.py:271-272, qualification/driver.py:287-291, production/descriptor.schema.json (properties.cache = {type: object})

Evidence: grep of production/ shows the only descriptor cache field consumed is d['cache']['package_root'] (harness.py:410). The archive hash is checked only in driver.py:271-272 against the driver's own constant CACHE_SHA, and the driver extracts with z.extractall (driver.py:291) without comparing to PACKAGE_MEMBER_MANIFEST.json, which cache.py hash-pins (line 15) but never parses (only CACHE_PAYLOAD_MANIFEST and UV_CACHE_SYMLINK_MAP are loaded, lines 112-113). The schema constrains cache only as an object.

- Refuter (bytes): **STANDS** → `NONBLOCKING_IMPROVEMENT`. Confirmed from bytes: production/harness.py:410 is the only production consumer of descriptor 'cache' and reads only package_root; descriptor.schema.json constrains cache as a bare {type: object} and validate_descriptor (harness.py:138-165) never inspects it; production/cache.py:15 hash-pins PACKAGE_MEMBER_MANIFEST.json but read_expected (96-147) parses only CACHE_PAYLOAD_MANIFEST and UV_CACHE_SYMLINK_MAP (112-113); qualification/driver.py:271 checks the archive against its own constant CACHE_SHA (line 21, the same constant it writes into the descriptor at line 233) and line 291 extractall()s …
- Refuter (charter): **REFUTED** → `OBSERVATION`. The factual observations are correct (production/harness.py:410 reads only cache.package_root; descriptor.schema.json leaves cache as a bare object; driver.py:271-272 checks the archive against its own constant and driver.py:291 extractall never consults PACKAGE_MEMBER_MANIFEST.json, which cache.py:15 pins but cache.py:112-113 never parse), but no controlling requirement is violated. CHARTER.md:129 states the harness-side obligation as verifying the realized cache (8,928 files / 31 symlinks / 481,715,990 bytes), which cache.py:194-216 enforces with per-member identities, exact path sets, …

### CG-03 — uv lock-basename derivation is exercised only by the driver's exact-artifact check; production neither rejects undeclared TMP writes nor requires declared stage files to exist
Reported `NONBLOCKING_IMPROVEMENT` · production/harness.py:273-277, production/harness.py:229-232, qualification/driver.py:322-341, history/F00/QP01_FAILED_RESULT.json (unexpected_artifacts), history/F00/QP01_STOP.json (error), qualification/uv_path_contract.py:35-56

Evidence: The F00 production stop names only cache members (QP01_STOP.json error: unexpected interpreter-v4/sdists-v9); the undeclared TMP lock uv-bf849b7789075826.lock appears only in the driver's unexpected_artifacts (QP01_FAILED_RESULT.json). In F01 the +5 lines (harness.py:273-277) observe a same-stage declared file only if it exists; no code walks D/E/L/O/TMP/MPL for undeclared files after readiness (only P and V are managed trees, harness.py:286). Recomputing uv_project_lock_basename for the five Q paths yields uv-bf849b7789075826 (F00), uv-03633e626eef7b72 (QP-01), uv-2391540581d1c5cd (QP-02), uv-c518f17c043ffbb5 (QN-05), uv-08f8a121be14c458 (QN-06), each matching the observed TMP artifact.

- Refuter (bytes): **STANDS** → `NONBLOCKING_IMPROVEMENT`. Confirmed from bytes: (1) production/harness.py:273-277 observes a declared same-stage file entry only under `and exists(entry['path'])`, so a wrongly derived uv_project_lock name (after='EXISTS_IF_STAGE_REACHED', stage UV_SYNC) would simply go unobserved with no GateError; (2) grep of production/ shows the only undeclared-file walks are Q (harness.py:214-216, PROVENANCE_GATE) and managed_observe over P/V (harness.py:286-306); D/E/L/O/TMP/MPL are never walked after readiness, and TMP's only check is the readiness-time nonempty test at harness.py:231; (3) the sole `found == required_set` check …
- Refuter (charter): **STANDS** → `NONBLOCKING_IMPROVEMENT`. Confirmed from the bytes. production/harness.py:274-277 observes a same-stage declared file only when `exists(entry['path'])` is true, and a read of the whole execute() path (lines 381-478) shows uv_project_lock is the sole kind=file write-set entry never passed to emit()/observe_output() (both raise on absence); no production code walks D/E/L/O/TMP/MPL for undeclared files (managed_observe at 280-324 covers only public_cache and virtual_environment). The only exact-artifact enforcement is qualification/driver.py:338-341 (`found == required_set`). history/F00/QP01_STOP.json names only …

**Observations (no action):**
- CG-04 — Optional-member mechanism cannot hide undeclared content; counts reconcile exactly
- CG-05 — QN-04 stop originates from production verify_cache, not from the wrapper or the bootstrap receipt
- CG-06 — Real-run consequence of a changed interpreter-cache key is an unconsumed pre-commit stop with re-entry refused
- CG-07 — Both derivations are scoped to the uv binary bytes, but production binds uv by version string only; cache_post_sync receipt carries the CACHE_BOOTSTRAP stage label

## Lens: preservation

Lens verdict: no MATERIAL_BLOCKER. S04 RUN01 is stated consumed/incomplete/not-evidence and the checkpoint un-materialized only in RETURN.md:33-34; STATE.json, LEASE_CLAIM.json (:4 "documented_predecessor_consumed_attempts": 5), LEASE_STATE.json and CASE_LEDGER_FINAL.json never name S04 RUN01 or 74ca21c1, and that hash appears nowhere outside controls/ (grep clean). Every commit/probe "checkpoint_sha256" carries the fixture hash 723d… (harness.py:335,360,396; driver.py:210), so no checkpoint was used. Protected-indicator scan: only "exchange_calendars" hits (2,100 across 20 members), all as public-cache member path names (archive-v0/…/exchange_calendars-4.11.1.dist-info) in descriptor required_members and D/cache_*.json; fixture depends only on pytest+4 (fixture/pyproject.toml:5-11). Arithmetic reconciles from bytes: 4 QUALIFICATION_CASE_COMMIT.json on disk, 0 RUN_ATTEMPT_COMMIT, 5 heartbeat STARTED lines (2+2+1), 6 = 5 + QN-06 failed Popen (STOP.json first_child_spawn_attempts 1, pids []), 9 outer = 8 + QN-06 replay (reentry_exit 73, reentry.stdout/stderr present), 9 instantiations = 8 F01 + 1 F00; 119 artifacts on disk. LEASE_CLAIM.json is assertion-only (no claim-file identity, mkdir result, or readback digest; no lease code in return), and every outer invocation had PID 7, so the "/proc namespace" quiescence scope is structurally near-vacuous. CUSTODY V0-V5 bind one libfile with 20/65/92/123/212/281 members; V5 = 14,066,292 B / 4f0fcf5c… matches RETURN.md:83-85 but is the WIP (281 > 220), not the sealed return; the return correctly does not claim to bind the outer zip (RETURN.md:88-89). 226 vs 228 = 228 leaves minus PACKAGE_MANIFEST.json and SANITIZATION.json. Runtime paths are consistent (…/bin/python, …/bin/uv) except STATE.json:131 "python3"; binary hashes are asserted post hoc, never bound in-run. No QA text found.

**Not verifiable from bytes:**
- Outer ZIP (9,486,845 B, 4c6efd4e...) binding claimed in Developer memory / rolling WIP / delivery status (RETURN.md:88-89) — no artifact inside the return can bind it; cannot be checked from bytes.
- That the lease exclusive mkdir, claim file, fsync and readback (LEASE_CLAIM.json:9) were actually performed — no claim-file bytes, path, hash, inode, or claiming code are in the return (driver.py only has per-case root mkdir at :277).
- That a /proc quiescence observation was actually performed and what it saw (LEASE_CLAIM.json:13-19, LEASE_STATE.json:1-9) — record-only.
- RUNTIME_PROVENANCE.json python/uv SHA-256 values — binaries absent; no in-run record binds them (only path+version checks in inherit.py:26-27, harness.py:192,398-400).
- Native Library readbacks V0-V5 (libfile_775f78fc..., backing file ids, versions, sizes, hashes) — platform metadata not reproducible offline.
- SANITIZATION.json actual scanned member list and pattern set (record gives count 226 and a prose scope only).
- RETURN.md:91-95 negative claims: which 'internal helpers' authored which components, and that no external QA repository was used — only absence of QA text is checkable (confirmed).
- F00 retired-candidate evidence: F00 descriptor (sha 52133c7b...), F00 driver.py (sha 0c6fd022...), F00 invocation.json/outer.stdout and the 13 F00 artifacts are hashed in history/F00/QP01_FAILED_RESULT.json but their bytes are not in the return.
- Equivalence of controls/S04_TERMINAL_STOP.json (sanitized packet control) to the charter §1 original stop record (libfile_044afdb8..., 8,913 B, b7e127fe...) — QA verified control bytes against known hashes; the return itself binds only the sanitized copy.

### CUST-01 — S04 stop / checkpoint preservation is asserted only in RETURN.md; structured records omit it and reuse the 'checkpoint_sha256' field for the fixture hash
Reported `NONBLOCKING_IMPROVEMENT` · RETURN.md:33-34, STATE.json:1-137, LEASE_CLAIM.json:4, LEASE_STATE.json:1-10, CASE_LEDGER_FINAL.json:110-130, production/harness.py:335

Evidence: grep for 74ca21c1, RUN01, LEASE-04, libfile_ac30da, 1f59b080, d700092b over all non-control members returns zero hits; 'S04' appears only at RETURN.md:33,35 and as the control filename in PACKAGE_MANIFEST.json:787. Every durable_probe.json, QUALIFICATION_CASE_COMMIT.json and supervisor probe carries checkpoint_sha256 = 723d254c... (the fixture tree hash, FROZEN_MANIFEST.json fixture_sha256), assigned at driver.py:210 and harness.py:335/360/396.

- Refuter (bytes): **STANDS** → `OBSERVATION`. The bytes confirm the finding's factual observations but not its weight. Confirmed: production/harness.py:335, :360 and :396 pass self.d['fixture_sha256'] into a field/argument named checkpoint_sha256, qualification/driver.py:210 seeds the durable probe the same way, and every QUALIFICATION_CASE_COMMIT.json / durable_probe.json carries checkpoint_sha256 = 723d254c... (the fixture hash). Also confirmed: no structured record (STATE.json, LEASE_CLAIM.json, LEASE_STATE.json, CASE_LEDGER_FINAL.json) names checkpoint 74ca21c1 or asserts NOT_MATERIALIZED; the only prose assertion is RETURN.md:33-34 …
- Refuter (charter): **REFUTED** → `OBSERVATION`. No controlling requirement supports the finding: CHARTER.md section 10 Q1 is a QA question, and STARTUP_ORDER.md section 8 (required return contents) contains no requirement for an explicit predecessor-state block in ledger/state/lease records. Preservation of the exact S04 stop is byte-level, not narrative: controls/S04_TERMINAL_STOP.json (8,913 B, sha256 b7e127fe...) is manifest-bound at PACKAGE_MANIFEST.json:787-790 and matches the charter section 1 binding. Absence of the checkpoint hash 74ca21c1 from every harness record is the charter-required state (STARTUP_ORDER.md section 4: …

### CUST-04 — LEASE_CLAIM.json evidences nothing beyond self-description; quiescence scope is a fresh PID namespace per invocation (every outer PID is 7)
Reported `NONBLOCKING_IMPROVEMENT` · LEASE_CLAIM.json:5-9, LEASE_CLAIM.json:13-19, LEASE_STATE.json:1-10, qualification/driver.py:276-277, case_results/F01/QP-01/result.json, case_results/F01/QN-06/result.json

Evidence: LEASE_CLAIM.json carries charter/startup hashes, claimed_utc, exclusive_parent path and the string ownership_method 'exclusive mkdir; exclusive claim file; file and parent fsync; exact local readback' but no claim-file path, size, SHA-256, inode, mkdir errno, or readback digest; no lease-claiming code exists in the return (driver.py only performs per-case roots['B'].mkdir(exist_ok=False) at :277). outer_pid is 7 in all eight F01 result.json files and in F00 QP01_FAILED_RESULT.json; the S04 supervisor child was PID 6. quiescence.scope is 'visible /proc namespace in this execution environment'; no /proc access exists in production/ or qualification/.

- Refuter (bytes): **STANDS** → `NONBLOCKING_IMPROVEMENT`. Every cited byte confirms the finding. LEASE_CLAIM.json:5-9 carries only exclusive_parent, lease_identity, owner and the prose string ownership_method 'exclusive mkdir; exclusive claim file; file and parent fsync; exact local readback'; :13-19 quiescence reports scope 'visible /proc namespace...' and hidden_other_chat_process_state NOT_OBSERVABLE. No field anywhere in the 220 members gives the claim file's path, size, SHA-256, inode, mkdir errno, or a readback digest: grep for lease/claim/proc/getpid across production/ and qualification/ finds only descriptor.schema.json's lease_identity …
- Refuter (charter): **REFUTED** → `OBSERVATION`. The finding's observations are accurate (LEASE_CLAIM.json:5-19 names no claim-file path/size/hash/readback digest; no lease-claiming code in production/ or qualification/; outer_pid 7 in all nine result records vs supervisor_child_pid 6 in S04_TERMINAL_STOP.json:30), but they do not constitute a gap against any stated requirement. STARTUP_ORDER.md:68 requires only that the claim and observed quiescence be recorded durably with the work product and read back before long execution; the return carries LEASE_CLAIM.json and LEASE_STATE.json, both bound by size/SHA-256 in PACKAGE_MANIFEST.json …

### CUST-06 — SANITIZATION.json count 226 vs 228 leaf members is explained by the manifest and the sanitization record post-dating the scan, but the record enumerates nothing
Reported `NONBLOCKING_IMPROVEMENT` · SANITIZATION.json:8-9, PACKAGE_MANIFEST.json:1-3, freezes/F01/PRODUCTION_HARNESS.zip, history/F00/PRODUCTION_HARNESS.zip

Evidence: On disk (excluding QA extraction dirs _f00/_f01 and the stray __pycache__): 220 files = 219 bound + PACKAGE_MANIFEST.json. Nested zips hold 5 + 5 members. 220 - 2 zips + 10 nested = 228 (Origin's count). 228 - PACKAGE_MANIFEST.json - SANITIZATION.json = 226, matching scanned_member_count_including_nested. SANITIZATION.json lists no member names, hashes, pattern set, or scan timestamp.

- Refuter (bytes): **STANDS** → `NONBLOCKING_IMPROVEMENT`. Every factual claim in CUST-06 checks out against the bytes. SANITIZATION.json:8 reads "scanned_member_count_including_nested": 226 and line 9 gives only a prose "scope" (categories of patterns, no pattern set); the file has no member list, no hashes, no timestamp, and no binding to the manifest. My independent recount (excluding _f00/_f01/__pycache__) gives 220 disk files, 2 zips each holding 5 leaves, so 228 leaves; 228 minus PACKAGE_MANIFEST.json and SANITIZATION.json = 226, and SANITIZATION.json is itself manifest-bound (so the manifest necessarily post-dates it), which makes the …
- Refuter (charter): **REFUTED** → `OBSERVATION`. The finding's facts are accurate (count 226 = 228 leaves minus the self-excluded manifest and the scan's own output; SANITIZATION.json lists no member names, hashes, pattern set, or timestamp), but its classification as a "real gap" rests on a requirement that does not exist in the controls. The sole sanitization requirement is controls/STARTUP_ORDER.md:105: "Scan the return for secrets and prohibited licensed/raw data; permitted protected contact and payload count is zero." SANITIZATION.json satisfies every stated element (scope at line 9, zero protected counts at lines 5-6, empty match list …

### CUST-07 — Runtime binary hashes in RUNTIME_PROVENANCE.json are never bound in-run; paths are consistent except STATE.json names a third spelling
Reported `NONBLOCKING_IMPROVEMENT` · RUNTIME_PROVENANCE.json:8-17, STATE.json:131, qualification/driver.py:23-24, production/inherit.py:26-27, production/harness.py:192, production/harness.py:398-400

Evidence: Every descriptor, invocation, and recorded process argv uses /opt/codex/.../python/bin/python (97 occurrences) and .../bin/uv (33); RUNTIME_PROVENANCE.json binds the same python path (realpath .../bin/python3.12) and uv path with SHA-256 021044... and 4f5e56.... inherit.py:26-27 and harness.py:192 compare sys.executable to the path and platform.python_version() to '3.12.13'; harness.py:398-400 compares `uv --version` output to the descriptor string. grep for 021044895e95be79 or 4f5e568281d9b5b4 in case_results/, case_specs/, production/, qualification/: no hits. STATE.json:131 gives python as .../bin/python3, a path used by no descriptor.

- Refuter (bytes): **STANDS** → `NONBLOCKING_IMPROVEMENT`. Confirmed on bytes. (1) The only occurrences of the python/uv content hashes 021044895e95be79... and 4f5e568281d9b5b4... in the entire return are in RUNTIME_PROVENANCE.json itself (grep -rl over all 220 members returns that single file); no script in production/ or qualification/ contains 'realpath', 'RUNTIME_PROVENANCE', or any hashing of sys.executable or the uv binary (grep over production/*.py and qualification/*.py: the only 'libc'/'realpath'-adjacent hits are harness.py:195 and driver.py:230, both platform-tuple comparisons), so there is no producer for that record inside the return and …
- Refuter (charter): **REFUTED** → `OBSERVATION`. The observations are byte-accurate (hashes 021044.../4f5e56... exist only in RUNTIME_PROVENANCE.json, which is manifest-bound at PACKAGE_MANIFEST.json:55-58; no in-run receipt or the commit binding at production/harness.py:332-340 carries runtime binary hashes; STATE.json:131 spells python as .../bin/python3, read or written by no code). But the defect is not a gap against any controlling requirement: CHARTER section 9's commit-binding list is "run identity, checkpoint, harness, descriptor, command, normalized environment, inventories, and passed readiness state" with no runtime binary …

### CUST-09 — STARTUP_ORDER §8 return-contents checklist: 12 PRESENT, 2 PARTIAL (sealed-return readback; secret scan enumeration), F00 retired evidence partial
Reported `NONBLOCKING_IMPROVEMENT` · freezes/F01/PRODUCTION_HARNESS.zip, freezes/F01/FROZEN_MANIFEST.json, production/descriptor.schema.json, qualification/driver.py, fixture/uv.lock, case_specs/F01/QP-01/descriptor.json

Evidence: Frozen production bundle PRESENT (zip 5 members + FROZEN_MANIFEST production map; production/ identical per QA). Schema PRESENT (production/descriptor.schema.json, also in zip). Driver PRESENT (driver.py + 4 helpers, hash-bound in FROZEN_MANIFEST qualification map). Fixture/lockfile PRESENT (4 files, fixture_sha256 723d... in every ledger/result). Per-case descriptors PRESENT (8, sizes 1,619,653-1,622,223 B, hashes match STATE/ledger/result/invocation). Ledger PRESENT (final + frozen with PLANNED_NOT_INVOKED rows). Commands/runtime/environment provenance PRESENT (descriptor commands/runtime/environment; SUCCESS/STOP processes with argv/cwd/pid/exit/timestamps; RUNTIME_PROVENANCE.json) with …

- Refuter (bytes): **STANDS** → `NONBLOCKING_IMPROVEMENT`. Bytes confirm every claim in CUST-09 and add one sharpening point. (1) Secret scan: SANITIZATION.json is a 10-key summary (status PASS, scanned_member_count_including_nested 226, matched_secrets_or_raw_rows [], scope as prose) with no member or pattern enumeration; moreover 226 does not reconcile to the sealed package (PACKAGE_MANIFEST binds 219 + 10 nested zip members = 229, 230 with the manifest), so at least four members of the sealed return were never covered by the recorded scan and, absent enumeration, cannot be identified. (2) Sealed-return readback: only CUSTODY_READBACK_V0..V5 exist …
- Refuter (charter): **STANDS** → `NONBLOCKING_IMPROVEMENT`. The finding survives only in narrowed form. Two of its three "partial" items are not defects under the controlling text: STARTUP_ORDER §8's sealed-return readback is by construction external to the return (STATE.json stage F01_8_OF_8_PASS_SEAL_PENDING / next_action "Seal sanitized return, independently read back" confirms ordering; CUSTODY_READBACK_V5.json supplies the in-return WIP receipt), and §8's return-contents list names "the frozen production bundle" (singular) and nowhere requires retired-candidate descriptor/driver bytes — the F00 production bytes are in fact present …

**Observations (no action):**
- CUST-02 — Protected-data indicator scan: only 'exchange_calendars' hits, all as public uv-cache member path names; fixture depends solely on pytest and its four dependencies
- CUST-03 — Attempt/commit/spawn arithmetic reconciles independently from on-disk artifacts; one ambiguous '9' and no stated grand total of outer invocations
- CUST-05 — Custody readbacks V0-V5 bind rolling WIP versions; V5 matches RETURN.md and is not the sealed return; no false outer-zip binding claim
- CUST-08 — Role separation: no external-QA text, diff, or brief appears in the return
- CUST-10 — QA-side contamination of the read-only extraction: qualification/__pycache__/uv_path_contract.cpython-311.pyc was created at 13:01 UTC on the QA host
- CUST-11 — Replay-rejection check is vacuous for the seven non-QN-06 cases; the exit-73 path is exercised exactly once, as the fixed matrix prescribes

## Lens: compliance

Charter sections 5, 7, 8 and 11 are substantively satisfied; no MATERIAL_BLOCKER found. Section 5 targets are singular (production/ 5 files; freezes/F01/PRODUCTION_HARNESS.zip + FROZEN_MANIFEST.json; one descriptor.schema.json; eight resolved descriptors; one driver plus 4 supplied child commands; fixture/ 3 files + manifest; CASE_LEDGER_FINAL.json; one WIP libfile across CUSTODY_READBACK_V0..V5). Every descriptor binds UV_PROJECT_ENVIRONMENT=V and harness.py:183-189 rejects otherwise; harness.py:430-431 and check_inputs :214-216 reject anything undeclared in Q; inputs_after equals inputs_before with exactly 3 Q files in QP-01/QP-02. Sync is --locked --offline --no-build under UV_OFFLINE=1 (driver.py:180-194); no network primitives in production/ or qualification/. The five fixture packages match descriptor cache required_members (.http sha256) and venv distributions; installed_projection matches in both venv_observation.json. Success cases carry SUCCESS.json and no STOP.json; negatives one STOP.json and no SUCCESS; driver.py:338-348 enforces exact required/forbidden manifests. QN-04 unlinks only inside its own P (bootstrap_missing_link.py:12-15); driver.py:271 re-hashes the shared archive before every case, so QN-05's PASS proves the archive intact after QN-04. QP-02 varies every component and the depth below the lease parent; production has no hardcoded paths, so stale binding is excluded, and a stronger variation would breach section 11. QN-05 is faithful to the section 7 allowance (child/test command returning nonzero) but exercises the collection stage, not the JUnit stage. The RUN-class/RUN_ATTEMPT_COMMIT path (harness.py:176-178, 331-340) is authorized prospective commit semantics but unexercised surface. Non-blocking gaps: E-absent constructed not observed, no whole-B undeclared-write sweep, descriptor cache.archive fields unconsumed, ledger binds commands only via descriptor hash, precommit re-entry stop not re-recorded, supervisor moved from direct FD redirection to pipe threads.

**Not verifiable from bytes:**
- Public cache archive bytes (wheels, archive-v0 unpacked contents, .http metadata, PUBLIC_WHEEL_SOURCE_IDENTITIES.json) are not in the return; only their SHA-256 values are bound (fixture/fixture_manifest.json, production/cache.py:13-19, descriptor inputs). Whether uv.lock wheel hashes correspond to …
- No measured post-matrix hash of the shared archive /workspace/scratch/cbd6528b11d8/inputs/...zip exists in the return; integrity after QN-06 is inferred from driver.py:271 pre-case checks (QN-05 ran after QN-04) and from QN-05/QN-06 perturbations being argv-only.
- E absent before readiness in QP-01/QP-02 is inferred from driver construction (driver.py:276-279 create B with exist_ok=False and only Q,T,R) and from invocation.json initial_evidence_state, which is copied from expected_artifacts.json (driver.py:308), not observed.
- Interpreter-cache key derivation (uv/interpreter-v4/e128016a93bda1d6/7f8106be2b9d4caa.msgpack) is declared UNKNOWN by the developer (UV_LOCK_DERIVATION.md:45-47); prefix invariance is only observed at two prefixes.
- Library custody identities, versions and the 14,066,292-byte WIP v5 hash (RETURN.md:82-85, CUSTODY_READBACK_V*.json) cannot be verified from the return bytes.
- Runtime binary hashes in RUNTIME_PROVENANCE.json (python, uv) cannot be verified; binaries are not present.
- Absence of network at runtime is evidenced only by flags/env (UV_OFFLINE=1, --offline, bootstrap receipt network_required=false); no network-namespace or socket evidence exists.
- That the same production bytes accept a RUN-class descriptor (charter section 5) is unexercised; harness.py:176-178 and 331-340 were never executed in any case, by charter design.
- Quiescence / hidden process state in other chats (LEASE_CLAIM.json quiescence.scope) is self-reported.

### CC-01 — E-absent precondition for positive cases is constructed, not observed or recorded
Reported `NONBLOCKING_IMPROVEMENT` · qualification/driver.py:276-279, qualification/driver.py:305-310, production/harness.py:222-234, case_results/F01/QP-01/invocation.json, case_results/F01/QP-01/artifacts/D/readiness.json

Evidence: driver.py:277 mkdir(B, exist_ok=False) and :278-279 create only Q,T,R; E is created by harness readiness (:225-228) which accepts an existing empty directory (before=ABSENT_OR_EXISTS_WRITABLE_EMPTY, :230). invocation.json initial_evidence_state='ABSENT' is copied from expected_artifacts.json at driver.py:308, not lstat-observed. readiness.json E entry records only post-construction type/mode/probe.

- Refuter (bytes): **STANDS** → `NONBLOCKING_IMPROVEMENT`. Confirmed from bytes: driver.py:277 mkdir(B, exist_ok=False) and :278-279 create only Q/T/R, with no pre-invocation lexists or lstat of E (the only pre-invocation existence checks at :301-303 cover S/C/P/V); harness.py:225-231 creates E if absent but accepts a pre-existing empty directory identically, and the receipt at :232-233 carries no created-vs-existed field; invocation.json's initial_evidence_state='ABSENT' is copied at driver.py:308 from expected_artifacts.json, which is a hardcoded literal at driver.py:243, so it is an expectation presented as observed state. No other artifact …
- Refuter (charter): **STANDS** → `OBSERVATION`. The factual claims hold from the bytes: driver.py:277-279 creates only B (exist_ok=False), Q, T, R; harness.py:225-234 constructs E and accepts an existing empty directory; the readiness receipt records only post-construction lstat/probe; driver.py:308 copies expected['initial_evidence_state'] into invocation.json without any lstat, and no returned artifact observes E's pre-state. However the charter obligation the finding relies on does not exist as stated: section 6 requires lstat observation AFTER construction and invalidates only receipts lacking post-construction observation (both …

### CC-02 — No whole-B sweep for undeclared writes outside the declared write set (T, B, R top level)
Reported `NONBLOCKING_IMPROVEMENT` · production/harness.py:214-216, production/harness.py:280-324, qualification/driver.py:322-331, qualification/driver.py:361-372, controls/CHARTER.md:156

Evidence: Harness verifies P and V exactly (managed_observe :280-324) and Q for undeclared files (:214-216, files only, empty directories invisible). Driver collects artifacts only under D,E,L,O,TMP,MPL (:323) and final observations only over declared write-set entries (:362). The control tree T (production code + 148MB extracted package), B root, and R root are never scanned post-run; input drift check covers only the 20 declared inputs.

- Refuter (bytes): **STANDS** → `NONBLOCKING_IMPROVEMENT`. Verified against the cited lines. production/harness.py:214-216 walks only roots['Q'] (rglob, files only); managed_observe (:280-324) os.walks only the entry's own path and is invoked solely for 'public_cache' and 'virtual_environment' (:410,:419,:424); cache.py:160-195 scandirs only P/uv; the only other iteration is the emptiness precheck of the six PRECOMMIT directories (:231). qualification/driver.py:323 collects artifacts only under D,E,L,O,TMP,MPL; :362 iterates only d['write_set']; the drift check at :336 rehashes only the 20 declared inputs plus the developer WORK tree (not T), and the …
- Refuter (charter): **STANDS** → `OBSERVATION`. The code facts are confirmed from bytes: nothing in production/harness.py or qualification/driver.py walks T, the B root, or the R root after a run, and the only T drift check is the 20 hash-bound inputs (harness.py:455-457, driver.py:336-337). However, the finding over-reads CHARTER.md:156, which obligates the descriptor to enumerate write targets and forbids certifying unlisted ones; it does not require a negative sweep of B, section 10 Q3 (:243) asks only that every declared entry be observed at its stage, and section 8 (:212) forbids unrelated hypothetical hardening. No code path writes …

### CC-03 — RUN-class commit path is unexercised surface; a mis-declared run_commit target would consume a real attempt
Reported `NONBLOCKING_IMPROVEMENT` · production/harness.py:176-178, production/harness.py:331-343, production/harness.py:125-136, controls/CHARTER.md:131, controls/CHARTER.md:212

Evidence: harness.py:331 selects commit_key='run_commit' for execution_class RUN; :341 write_exclusive then :343 observe_output, which raises WRITE_SET_GATE if the entry's after != EXISTS_IF_STAGE_REACHED (:128-129). All eight descriptors declare run_commit as kind protected/ABSENT (driver.py:173-176). No case executes the RUN branch.

- Refuter (bytes): **STANDS** → `NONBLOCKING_IMPROVEMENT`. Every cited line is accurate: production/harness.py:331 selects commit_key='run_commit' for execution_class RUN; :341 write_exclusive (which fsyncs file+parent and reads back, i.e. a durable RUN_ATTEMPT_COMMIT per CHARTER section 9) runs BEFORE :343 observe_output, whose :128-129 check raises WRITE_SET_GATE whenever the entry's 'after' != 'EXISTS_IF_STAGE_REACHED'. Nothing upstream prevents this for a mis-declared RUN descriptor: validate_descriptor (:170-178) only checks run_identity is nonempty for RUN and has no class-conditional check on the commit target's kind/after; readiness() …
- Refuter (charter): **REFUTED** → `OBSERVATION`. The finding is refuted as a NONBLOCKING_IMPROVEMENT against this return; what survives is an OBSERVATION for the future RUN-descriptor author. (1) "Unexercised surface" is charter-mandated, not a gap in the return: CHARTER.md:174 ("No case may create RUN_ATTEMPT_COMMIT... Any non-null run identity or RUN_ATTEMPT_COMMIT during HQ is a defined stop") and STARTUP_ORDER.md:97 ("RUN_ATTEMPT_COMMIT is prohibited in HQ01") forbid exercising that branch; CHARTER.md:131 only requires the bytes to "later accept a real-run descriptor", and CHARTER.md:246 routes that to a separate Origin RUN01 order. (2) …

### CC-04 — Protected-root absence in QUALIFICATION class is enforced only by descriptor-supplied protected entries, not by a class-level invariant
Reported `NONBLOCKING_IMPROVEMENT` · production/harness.py:200-203, production/harness.py:235-240, qualification/driver.py:169-171, qualification/driver.py:346, controls/CHARTER.md:154

Evidence: check_protected (:200-203) iterates only write-set entries with kind=='protected'. Roots S and C are checked for topology only (:150-159). Their absence is enforced because driver.py:169-171 emits S/C as protected entries and driver.py:346 independently checks lexists; the schema (descriptor.schema.json) does not constrain S/C kind.

- Refuter (bytes): **STANDS** → `NONBLOCKING_IMPROVEMENT`. Confirmed from the bytes, line numbers accurate. production/harness.py:200-203 check_protected() iterates only write-set entries with kind=='protected'; :237-239 (readiness) does the same; validate_descriptor (:145-161) checks roots S/C solely for key set, absolute/real path, containment and topology, never for absence; the only other references to S/C anywhere in production/ (grep over harness.py, supervisor.py, inherit.py, cache.py) are none, so no production code enforces S/C absence by root key. descriptor.schema.json constrains roots S/C only as {"type":"string"} and the write_set items …
- Refuter (charter): **REFUTED** → `OBSERVATION`. The finding's factual observations are accurate (production/harness.py:200-203 and :237-239 check only write-set entries with kind=='protected'; descriptor.schema.json:27-38 does not force S/C to appear as protected entries), but the requirement it relies on -- that protected-root absence be a production class-level invariant independent of the descriptor -- does not exist in the controlling text, and the charter mandates the opposite design. CHARTER.md:156 states "The launcher, readiness logic, and receipts must consume that same write-set manifest rather than maintain copied path lists"; …

### CC-05 — QN-05 exercises a nonzero exit at the collection stage, not the JUnit test stage
Reported `NONBLOCKING_IMPROVEMENT` · qualification/fail_child.py:1-10, qualification/driver.py:204-205, production/harness.py:346-367, RETURN.md:23-24, controls/CHARTER.md:184, controls/CHARTER.md:195

Evidence: fail_child.py runs pytest.main(--collect-only) in-process then exits 23; driver.py:205 substitutes it for the collection argv. stage_status.jsonl records stage COLLECTION, pid 26, exit 23; no collection_check.json, pytest.stdout or junit.xml exist (expected_artifacts forbidden list). RETURN.md:23 says returns 23 after collection, which matches the bytes.

- Refuter (bytes): **REFUTED** → `OBSERVATION`. The bytes confirm the description but not the defect. (1) The charter itself defines QN-05 as "synthetic pytest child deliberately exits nonzero" (controls/CHARTER.md:184) and "immutable qualification child/test command that returns nonzero" (CHARTER.md:195); STARTUP_ORDER.md:94 says "Synthetic child exits nonzero". Nothing in the controls requires the JUnit-producing test stage to be the failing child, and CHARTER.md section 8 (line ~203) fixes "exactly two positive and six negative cases; no case may be added without a demonstrated uncovered failure and new Origin authority" and forbids …
- Refuter (charter): **REFUTED** → `OBSERVATION`. The finding's facts are correct (QN-05 exits 23 at stage COLLECTION, pid 26; no test stage runs) but the requirement it measures against does not exist: CHARTER.md:184 says only "synthetic pytest child deliberately exits nonzero ... no later stage starts", CHARTER.md:195 says "immutable qualification child/test command that returns nonzero" (disjunctive), and STARTUP_ORDER.md:94 says "Synthetic child exits nonzero"; none names the JUnit/test stage. The charter's own post-commit semantics centre on the collection child (CHARTER.md:225 "exactly one OS spawn attempt of the bound hermetic …

### CC-06 — Descriptor cache.archive / archive_sha256 fields are not consumed by production; cache identity is bound by constants in cache.py
Reported `NONBLOCKING_IMPROVEMENT` · production/cache.py:12-21, production/harness.py:410-411, qualification/driver.py:233, controls/CHARTER.md:131

Evidence: grep of production/ finds no reference to d['cache']['archive'] or archive_sha256; harness.py:410 uses only package_root. cache.py:13-19 hardcodes five control hashes, :20-21 the counts (including directories=839 not stated in the charter).

- Refuter (bytes): **STANDS** → `OBSERVATION`. The narrow fact stands: production/ contains no reference to d['cache']['archive'] or archive_sha256 (grep: zero hits), harness.py:410 passes only package_root to verify_cache, and cache.py:12-21 hardcodes the five control hashes and counts (incl. directories=839). But the material half of the finding -- that cache identity is bound only by constants and the descriptor is decorative for it -- is refuted: every descriptor's 20-entry inputs map hash-binds the five cache control files under package_root, and harness.py:205-213 check_inputs verifies them at PROVENANCE_GATE at three boundaries …
- Refuter (charter): **REFUTED** → `OBSERVATION`. The factual observation is correct (production never reads d['cache']['archive']/archive_sha256; cache.py:13-21 hardcodes five control hashes and counts incl. directories=839), but the inferred defect does not hold against the bytes or the controlling text. (1) The zip is not a production input: every descriptor's bootstrap argv runs <package_root>/bootstrap_uv_cache.py --package-root <package_root>; only qualification/driver.py:271 opens the zip, and it verifies size 148,782,742 + sha256 c4be6336... before extractall (:287-290). cache.archive/archive_sha256 are driver provenance, and …

### CC-07 — Precommit re-entry of a stopped root would not record a second truthful stop
Reported `NONBLOCKING_IMPROVEMENT` · production/harness.py:383-387, production/harness.py:475-478, production/harness.py:245-248

Evidence: execute() only short-circuits on an existing commit (:383). For a QN-01..QN-04 style root re-invoked, readiness emit (:247) hits write_exclusive FileExistsError; the except path at :475 skips emitting because STOP.json already exists and only prints to stdout, returning 1.

- Refuter (bytes): **REFUTED** → `OBSERVATION`. The cited mechanism does not occur and the residual behaviour is contract-conformant fail-closed refusal, not a defect. (1) Mechanism: STOP.json is a write-set target under root D (case_specs/F01/QP-01/descriptor.json: id 'stop' -> runtime/diagnostic/STOP.json), and D is a PRECOMMIT directory entry with before='ABSENT_OR_EXISTS_WRITABLE_EMPTY'. On re-invocation of a QN-01..QN-04 root, production/harness.py execute() :383 passes (no commit), then readiness() :222-232 iterates D first, finds it exists and is nonempty (it holds STOP.json, and for QN-02..04 also …
- Refuter (charter): **REFUTED** → `OBSERVATION`. The requirement the finding relies on does not exist as stated: CHARTER.md:191 requires "one truthful stop record" per failing CASE in the fixed section-7 matrix, whose only re-entry case is the postcommit QN-06 (CHARTER.md:185,195); precommit re-entry is not a case, and section 8 / STARTUP_ORDER.md:97 forbid adding cases or hypothetical hardening. The cited mechanism is also wrong: on re-invocation of a QN-01..QN-04 root, readiness() iterates PRECOMMIT directories in descriptor order D,L,O,TMP,MPL,E, and D (which already holds STOP.json in every stopped root) trips the …

### CC-08 — Generalized supervisor replaced direct child FD redirection with pipes and copy threads
Reported `NONBLOCKING_IMPROVEMENT` · SUPERVISOR_GENERALIZATION.diff, production/supervisor.py:218-226, production/supervisor.py:258-279, controls/CHARTER.md:131

Evidence: Donor s02_supervisor.py:143-149 passed stdout=log_stream, stderr=STDOUT (kernel-level redirection). Generalized :218-226 uses stdout/stderr=PIPE with two reader threads (:258-279) that terminate the child on copy failure (:269).

- Refuter (bytes): **REFUTED** → `OBSERVATION`. The cited lines are accurate (production/supervisor.py:218-226 PIPE, :258-279 copy threads, :269 terminate; donor :143-149 stdout=log_stream/stderr=STDOUT), but the "weakening of subprocess semantics" claim does not survive the bytes. controls/CHARTER.md:164 and :187 require distinct absolute stdout, stderr, AND supervisor-log targets, which the donor's merged kernel-level redirection cannot produce from one child; a parent-side relay is therefore charter-mandated, not a discretionary design change. Fail-closed behavior is strengthened: copy failure terminates the child, records …
- Refuter (charter): **REFUTED** → `OBSERVATION`. The finding's premise that the pipe/thread capture is a weakening of subprocess semantics under CHARTER.md:131 is not supported by the charter text or the artifacts. CHARTER.md:164, :184 (QN-05) and :187 require separate absolute stdout and stderr targets plus a supervisor log, and every descriptor binds distinct *_stdout/*_stderr write-set entries; the donor's stdout=log_stream/stderr=STDOUT (EXACT_DONOR_SUPERVISOR.py:143-149) could not satisfy that, so the plumbing change was charter-required. The generalized code (production/supervisor.py:218-226, :258-279, :309-317) preserves the donor's …

**Observations (no action):**
- CC-09 — Case ledger binds sync/pytest commands only transitively through descriptor hashes
- CC-10 — QP-02 alternate prefix shares the lease parent; stronger variation would violate section 11
- CC-11 — Driver spawn/commit counts rely on harness self-report but are independently checkable from artifacts
- CC-12 — Input staging directory outside the lease parent
- CC-13 — Section 8 anti-sprawl and section 11 prohibitions otherwise hold
- CC-14 — Stray CPython 3.11 bytecode in extracted qualification/ is not part of the return

